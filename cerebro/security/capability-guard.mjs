const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function checkCapability(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','environment','version'])req(c[k],`context.${k}`);req(i.capability_id,'capability_id');req(i.action,'action');const allowed=Array.isArray(i.allowed_capabilities)?i.allowed_capabilities:[];if(!allowed.includes(i.capability_id))return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',allowed:false});if(i.cross_company===true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',allowed:false});if(i.high_risk===true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK',allowed:false});return Object.freeze({status:'ALLOW',allowed:true,company_id:c.company_id,capability_id:i.capability_id,action:i.action,audit_required:true})}
