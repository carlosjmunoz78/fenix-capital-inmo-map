const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function evaluateKillSwitch(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','environment','version'])req(c[k],`context.${k}`);const global=i.global_kill===true;const ids=[i.identity_kill,i.account_kill,i.connector_kill].filter(Boolean);if(global||ids.length>0)return Object.freeze({status:'BLOCKED',reason:'SECURITY_INCIDENT',allow:false,revocation_required:true,scope:global?'GLOBAL':'SCOPED',audit_required:true});return Object.freeze({status:'ALLOW',allow:true,revocation_required:false,audit_required:true})}
