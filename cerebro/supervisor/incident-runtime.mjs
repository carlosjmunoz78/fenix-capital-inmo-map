const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
const CANON=new Set(['LEGAL_REQUIRED','SIGNATURE_REQUIRED','LOW_CONFIDENCE','HIGH_RISK','POLICY_CONFLICT','SECURITY_INCIDENT','MONEY_LIMIT','CUSTOMER_HUMAN_REQUEST']);
export function classifyIncident(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','environment','version'])req(c[k],`context.${k}`);if(c.environment!=='PREPROD')return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK'});req(i.incident_id,'incident_id');const severity=req(i.severity,'severity');const requested=typeof i.reason==='string'?i.reason.trim():'';const reason=CANON.has(requested)?requested:(severity==='CRITICAL'?'SECURITY_INCIDENT':'HIGH_RISK');return Object.freeze({status:'HUMAN_REQUIRED',reason,incident_id:i.incident_id,company_id:c.company_id,containment_required:true,prod_write_allowed:false,trading_mutation_allowed:false,audit_required:true})}
