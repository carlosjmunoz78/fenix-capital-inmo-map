const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
const SAFE=new Set(['RETRY','RESTART_WORKER','REQUEUE_JOB','REBUILD_CACHE','REOPEN_LOCAL_STORE']);
export function planHealing(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','engine_id','environment','version'])req(c[k],`context.${k}`);if(c.environment!=='PREPROD')return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK',execute:false});const action=req(i.action,'action');if(i.trading===true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',execute:false});if(i.prod_write===true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK',execute:false});if(i.additional_cost_eur!=null&&Number(i.additional_cost_eur)>0)return Object.freeze({status:'HUMAN_REQUIRED',reason:'MONEY_LIMIT',execute:false});if(!SAFE.has(action))return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK',execute:false});return Object.freeze({status:'PLAN_READY',action,execute:false,preprod_only:true,rollback_required:true,verification_required:true,audit_required:true})}
