import {evaluateLabCandidate} from './lab-core.mjs';
export function evaluateMarketingLab(i={}){if(i.context?.engine_id!=='LAB-MKT')return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',promote_to_prod:false});const r=evaluateLabCandidate(i);if(r.status!=='LAB_CANDIDATE_VALIDATED')return r;const test=i.candidate?.test_type;if(!['CREATIVE','AUDIENCE','LANDING','COPY','OFFER'].includes(test))return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',promote_to_prod:false});const budget=Number(i.candidate?.microbudget_eur??0);if(!Number.isFinite(budget)||budget<0)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',promote_to_prod:false});if(budget>0)return Object.freeze({status:'HUMAN_REQUIRED',reason:'MONEY_LIMIT',promote_to_prod:false});return Object.freeze({...r,test_type:test,metric_required:true,learning_capture:true,marketing_write:false,spend_execute:false})}
