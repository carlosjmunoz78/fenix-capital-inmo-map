import {evaluateLabCandidate} from './lab-core.mjs';
export function evaluateSeoLab(i={}){if(i.context?.engine_id!=='LAB-SEO')return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',promote_to_prod:false});const r=evaluateLabCandidate(i);if(r.status!=='LAB_CANDIDATE_VALIDATED')return r;const experiment=i.candidate?.experiment;if(!['TITLE','META','INTERNAL_LINKING','CONTENT','SCHEMA','LOCAL'].includes(experiment))return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',promote_to_prod:false});return Object.freeze({...r,experiment,metric_required:true,seo_write:false})}
