export function evaluateLabCandidate({context,candidate={},min_confidence=.7,max_cost_eur=0}={}){if(!context||typeof context!=='object')throw new Error('context required');for(const k of ['company_id','engine_id','environment','version'])if(typeof context[k]!=='string'||!context[k].trim())throw new Error(`context.${k} required`);if(context.environment!=='LAB')return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',promote_to_prod:false});const confidence=Number(candidate.confidence);if(!Number.isFinite(confidence)||confidence<min_confidence)return Object.freeze({status:'HUMAN_REQUIRED',reason:'LOW_CONFIDENCE',promote_to_prod:false});const cost=Number(candidate.estimated_cost_eur??0);if(!Number.isFinite(cost)||cost>max_cost_eur)return Object.freeze({status:'HUMAN_REQUIRED',reason:'MONEY_LIMIT',promote_to_prod:false});if(candidate.high_risk===true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK',promote_to_prod:false});return Object.freeze({status:'LAB_CANDIDATE_VALIDATED',promote_to_prod:false,external_write:false,executed:false,read_only:true,additional_cost_eur:cost})}
