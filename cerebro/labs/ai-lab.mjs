import {evaluateLabCandidate} from './lab-core.mjs';
export function evaluateAiLab(i={}){if(i.context?.engine_id!=='LAB-AI')return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',promote_to_prod:false});const r=evaluateLabCandidate(i);if(r.status!=='LAB_CANDIDATE_VALIDATED')return r;const kind=i.candidate?.kind;if(!['MODEL','PROMPT','TOOL_USE','ROUTING'].includes(kind))return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',promote_to_prod:false});if(i.candidate?.uses_real_secrets===true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'SECURITY_INCIDENT',promote_to_prod:false});if(i.candidate?.synthetic_dataset!==true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',promote_to_prod:false});return Object.freeze({...r,kind,synthetic_dataset:true,security_review_required:true,model_integration_execute:false,paid_ai_required:false})}
