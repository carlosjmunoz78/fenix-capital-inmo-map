import {evaluateLabCandidate} from './lab-core.mjs';
export function evaluateAutomationLab(i={}){if(i.context?.engine_id!=='LAB-AUT')return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',promote_to_prod:false});const r=evaluateLabCandidate(i);if(r.status!=='LAB_CANDIDATE_VALIDATED')return r;const trigger=i.candidate?.trigger;if(!['EVENT','SCHEDULE','CONDITION','MANUAL'].includes(trigger))return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',promote_to_prod:false});if(i.candidate?.synthetic_fixture!==true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',promote_to_prod:false});const failureMode=i.candidate?.failure_mode??'NONE';if(!['NONE','TIMEOUT','RETRY','DEPENDENCY_FAILURE','DUPLICATE_EVENT'].includes(failureMode))return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',promote_to_prod:false});return Object.freeze({...r,trigger,failure_mode:failureMode,synthetic_fixture:true,workflow_execute:false,external_send:false,prod_writes:false})}
