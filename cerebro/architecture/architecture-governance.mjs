const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function evaluateArchitectureGovernance(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','engine_id','environment','version'])req(c[k],`context.${k}`);if(c.environment!=='PREPROD')return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK',ready:false});if(i.trading===true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',ready:false});const deps=Array.isArray(i.dependencies)?i.dependencies:[];const seen=new Set();for(const d of deps){if(!d||typeof d!=='object'||Array.isArray(d))throw new Error('dependency object required');for(const k of ['from_engine_id','to_engine_id','company_id','environment','version'])req(d[k],`dependency.${k}`);if(d.company_id!==c.company_id)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',ready:false});if(d.environment==='PROD')return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK',ready:false});const key=`${d.from_engine_id}>${d.to_engine_id}`;if(seen.has(key))return Object.freeze({status:'BLOCKED',reason:'POLICY_CONFLICT',ready:false,issue:'DUPLICATE_DEPENDENCY'});seen.add(key)}const debt=Array.isArray(i.debt_items)?i.debt_items:[];for(const d of debt){if(!d||typeof d!=='object'||Array.isArray(d))throw new Error('debt object required');for(const k of ['debt_id','owner','status','rollback_ref'])req(d[k],`debt.${k}`);if(!['OPEN','MITIGATED','CLOSED'].includes(d.status))throw new Error('invalid debt status')}const gates={inventory:i.inventory===true,dependency_map:i.dependency_map===true,current_contract:i.current_contract===true,backup:i.backup===true,tests:i.tests===true,rollback:i.rollback===true,changelog:i.changelog===true,rebuild:i.rebuild===true};const missing=Object.entries(gates).filter(([,v])=>!v).map(([k])=>k);if(missing.length)return Object.freeze({status:'BLOCKED',reason:'POLICY_CONFLICT',ready:false,missing:Object.freeze(missing)});return Object.freeze({status:'ARCHITECTURE_READY',ready:true,company_id:c.company_id,dependency_count:deps.length,debt_open:debt.filter(x=>x.status==='OPEN').length,prod_promotion:false,audit_required:true,gates:Object.freeze(gates)})}
