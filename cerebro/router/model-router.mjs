const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function routeWork(i={}){req(i.task_type,'task_type');const deterministic=new Set(['sql','python','typescript','rule','api','local_tool','engine_call']);const interpretation=new Set(['interpretation','reasoning','comparison','redaction','research']);if(deterministic.has(i.task_type))return Object.freeze({status:'ROUTED',target:i.task_type,ai_required:false,cost_target_eur:0});if(interpretation.has(i.task_type)){const free=Array.isArray(i.available_free_models)?i.available_free_models.filter(Boolean):[];if(free.length>0)return Object.freeze({status:'ROUTED',target:free[0],ai_required:true,cost_target_eur:0});const paid=Number(i.estimated_paid_cost_eur??0);if(!Number.isFinite(paid)||paid<0)throw new Error('estimated_paid_cost_eur invalid');if(paid>0)return Object.freeze({status:'HUMAN_REQUIRED',reason:'MONEY_LIMIT',ai_required:true});return Object.freeze({status:'BLOCKED',reason:'POLICY_CONFLICT',ai_required:true})}return Object.freeze({status:'BLOCKED',reason:'LOW_CONFIDENCE'})}
