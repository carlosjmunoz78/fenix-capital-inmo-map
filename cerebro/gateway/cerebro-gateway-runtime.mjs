const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function routeGatewayRequest(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','environment','version'])req(c[k],`context.${k}`);req(i.request_id,'request_id');req(i.intent,'intent');if(i.direct_model_access===true)return Object.freeze({status:'BLOCKED',reason:'POLICY_CONFLICT',executed:false});const cost=Number(i.estimated_additional_cost_eur??0);if(!Number.isFinite(cost)||cost<0)throw new Error('estimated_additional_cost_eur invalid');if(cost>0)return Object.freeze({status:'HUMAN_REQUIRED',reason:'MONEY_LIMIT',executed:false});return Object.freeze({status:'ROUTE_READY',gateway:true,request_id:i.request_id,intent:i.intent,context:structuredClone(c),requires:['context','policy','permission','router','audit'],executed:false,additional_cost_target_eur:0})}
