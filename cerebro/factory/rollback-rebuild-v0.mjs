const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function verifyRollbackRebuildV0(i){const c=i?.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','engine_id','environment','version'])req(c[k],`context.${k}`);const backup=req(i?.backup_ref,'backup_ref');const rollback=req(i?.rollback_ref,'rollback_ref');const rebuild=req(i?.rebuild_ref,'rebuild_ref');const snapshot=req(i?.snapshot_ref,'snapshot_ref');if(i?.restore_test_passed!==true)return Object.freeze({status:'BLOCKED',reason:'POLICY_CONFLICT',missing:['restore_test_passed'],ready:false,context:structuredClone(c)});if(i?.rollback_test_passed!==true)return Object.freeze({status:'BLOCKED',reason:'POLICY_CONFLICT',missing:['rollback_test_passed'],ready:false,context:structuredClone(c)});if(i?.rebuild_test_passed!==true)return Object.freeze({status:'BLOCKED',reason:'POLICY_CONFLICT',missing:['rebuild_test_passed'],ready:false,context:structuredClone(c)});return Object.freeze({status:'RESILIENCE_READY',ready:true,backup_ref:backup,rollback_ref:rollback,rebuild_ref:rebuild,snapshot_ref:snapshot,restore_test_passed:true,rollback_test_passed:true,rebuild_test_passed:true,prod_writes:false,context:structuredClone(c)})}
