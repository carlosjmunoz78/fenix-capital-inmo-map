export function planInvoice(input={}){const {context={},authorized=false,requires_prod_write=false,confidence=0,source_refs=[],invoice={}}=input;if(!context.company_id||context.engine_id!=='INV-001'||!['LAB','PREPROD'].includes(context.environment))throw new Error('UNSAFE_CONTEXT');if(!Array.isArray(source_refs)||!source_refs.length)throw new Error('EVIDENCE_REQUIRED');if(!authorized)return human('POLICY_CONFLICT');if(requires_prod_write)return human('HIGH_RISK');if(confidence<.8)return human('LOW_CONFIDENCE');const net=Number(invoice.net_cents??NaN),vat=Number(invoice.vat_cents??NaN);if(!Number.isInteger(net)||!Number.isInteger(vat))throw new Error('INTEGER_CENTS_REQUIRED');if(invoice.tax_reviewed!==true)return human('LEGAL_REQUIRED');const total=net+vat;return {status:'INVOICE_PLAN_READY',engine_id:'INV-001',company_id:context.company_id,invoice_ref:invoice.invoice_ref??null,expediente_ref:invoice.expediente_ref??null,net_cents:net,vat_cents:vat,total_cents:total,issue_execute:false,accounting_export_execute:false,collection_link_plan:true,read_only:true,plan_only:true,executed:false,prod_writes:false,cost_additional_eur:0,source_refs:[...source_refs]}}function human(reason){return {status:'HUMAN_REQUIRED',reason,read_only:true,plan_only:true,executed:false,prod_writes:false}}