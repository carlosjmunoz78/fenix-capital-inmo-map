const SAFE_ENV=new Set(['SCAFFOLD','LAB','PREPROD']);
function req(v,l){if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim();}
function ctx(i){const c=i?.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','engine_id','environment','version'])if(!Object.prototype.hasOwnProperty.call(c,k))throw new Error(`context.${k} required`);const o=Object.freeze({company_id:req(c.company_id,'company_id'),engine_id:req(c.engine_id,'engine_id'),environment:req(c.environment,'environment'),version:req(c.version,'version')});if(o.engine_id!=='BNK-001')throw new Error('engine_id must be BNK-001');if(!SAFE_ENV.has(o.environment))throw new Error('unsafe environment');return o;}
function human(reason,context){return Object.freeze({status:'HUMAN_REQUIRED',reason,context,executed:false});}
export function normalizeBankKnowledge(i={}){const context=ctx(i);if(i.requires_prod_write===true)return human('HIGH_RISK',context);if(i.authorized!==true)return human('POLICY_CONFLICT',context);const bank=req(i.bank,'bank');const criterion=req(i.criterion,'criterion');const source_ref=req(i.source_ref,'source_ref');const effective_date=req(i.effective_date,'effective_date');if(typeof i.confidence!=='number'||!Number.isFinite(i.confidence)||i.confidence<0||i.confidence>1)throw new Error('confidence invalid');if(i.confidence<0.7)return human('LOW_CONFIDENCE',context);if(!Number.isInteger(i.case_count)||i.case_count<0)throw new Error('case_count invalid');return Object.freeze({status:'BANK_KNOWLEDGE_READY',context,bank,criterion,source_ref,effective_date,confidence:i.confidence,case_count:i.case_count,review_required_for_material_change:true,knowledge_promotion:false,update_via:['KNW-001','RSH-001','UPD-001','PRV-001'],read_only:true,plan_only:true,executed:false,prod_writes:false,additional_cost_target_eur:0});}
