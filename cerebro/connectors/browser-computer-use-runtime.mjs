const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function planBrowserComputerUse(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','identity_id','account_id','environment','version'])req(c[k],`context.${k}`);if(!['LAB','PREPROD'].includes(c.environment))return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK',ready:false});for(const k of ['request_id','provider','profile_id','action'])req(i[k],k);if(i.api_or_connector_available===true)return Object.freeze({status:'BLOCKED',reason:'POLICY_CONFLICT',ready:false,preferred_route:'API_OR_CONNECTOR'});if(i.bypass_captcha===true||i.bypass_mfa===true||i.evade_platform_controls===true)return Object.freeze({status:'BLOCKED',reason:'SECURITY_INCIDENT',ready:false});if(i.human_presence_required===true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',ready:false});const cost=Number(i.estimated_additional_cost_eur??0);if(!Number.isFinite(cost)||cost<0)throw new Error('invalid cost');if(cost>0)return Object.freeze({status:'HUMAN_REQUIRED',reason:'MONEY_LIMIT',ready:false});return Object.freeze({status:'RUNTIME_PLAN_READY',ready:true,mode:'CONTROLLED_BROWSER_FALLBACK',isolated_profile:true,audit_required:true,verify_result:true,request_id:i.request_id,provider:i.provider,profile_id:i.profile_id,action:i.action,executed:false,additional_cost_target_eur:0})}
