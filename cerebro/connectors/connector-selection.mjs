const ORDER=['API','MCP','WEBHOOK','SCRIPT','CLI','BROWSER','COMPUTER_USE','FACTORY_BUILD'];
export function selectConnector(i={}){const available=Array.isArray(i.available_routes)?i.available_routes:[];const costBy=i.cost_by_route||{};for(const type of ORDER){const r=available.find(x=>x?.type===type&&x?.authorized===true&&x?.status==='ACTIVE');if(!r)continue;const cost=Number(costBy[type]??r.estimated_additional_cost_eur??0);if(!Number.isFinite(cost)||cost<0)continue;if(cost>0)continue;return Object.freeze({status:'ROUTED',route:type,connector_id:r.connector_id??null,cost_target_eur:0,fallback_order:Object.freeze([...ORDER]),verify_required:true})}const paid=available.find(x=>x?.authorized===true&&x?.status==='ACTIVE'&&Number(x?.estimated_additional_cost_eur??0)>0);if(paid)return Object.freeze({status:'HUMAN_REQUIRED',reason:'MONEY_LIMIT',route:null});return Object.freeze({status:'ROUTE_GAP',reason:'POLICY_CONFLICT',route:'FACTORY_BUILD',factory_build_required:true})}
