import {runTrainingCycle} from './training-runtime-v0.mjs';
const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function buildTrainingEvidence(i={}){const r=runTrainingCycle(i);if(r.status==='HUMAN_REQUIRED')return r;req(i.evidence_ref,'evidence_ref');req(i.dataset_manifest_ref,'dataset_manifest_ref');return Object.freeze({schema_version:'0.1.0',status:'EVIDENCE_READY',context:Object.freeze({...i.context}),run_id:r.run_id,dataset_version:r.dataset_version,dataset_manifest_ref:i.dataset_manifest_ref,evidence_ref:i.evidence_ref,code_version:i.code_version,config_version:i.config_version,candidate_version:r.candidate_version,champion_version:r.champion_version,metrics:Object.freeze({champion_score:r.champion_score,challenger_score:r.challenger_score,delta:r.delta}),evaluation:r.evaluation,judge:r.judge,champion_challenger:r.champion_challenger,promotion_decision:r.judge.decision,promotion_target:r.promotion_target,prod_promotion:false,gcp_required:false,trading_access:false,additional_cost_target_eur:0,rollback_required:true,rebuild_required:true,audit_required:true})}
export function serializeTrainingEvidence(i={}){return `${JSON.stringify(buildTrainingEvidence(i))}\n`}
