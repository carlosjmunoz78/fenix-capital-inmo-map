const REQUIRED_CONTEXT=['company_id','engine_id','environment','version'];
function nonEmpty(v,l){if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim();}
function ctx(input){const c=input?.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of REQUIRED_CONTEXT)nonEmpty(c[k],`context.${k}`);if(c.engine_id!=='WAUD-001')throw new Error('engine_id must be WAUD-001');if(c.environment!=='SCAFFOLD')throw new Error('environment must be SCAFFOLD');return structuredClone(c);}
function human(reason,c){return Object.freeze({status:'HUMAN_REQUIRED',reason,context:c,executed:false});}
export function planWebsiteAudit(input){const c=ctx(input);const url=nonEmpty(input?.url,'url');let u;try{u=new URL(url);}catch{throw new Error('invalid url');}if(!['http:','https:'].includes(u.protocol))throw new Error('invalid protocol');if(input?.authorized!==true)return human('POLICY_CONFLICT',c);if(input?.requires_prod_write===true)return human('HIGH_RISK',c);const cost=Number(input?.estimated_additional_cost_eur??0);if(!Number.isFinite(cost)||cost<0)throw new Error('invalid cost');if(cost>0)return human('MONEY_LIMIT',c);return Object.freeze({status:'PLAN_READY',engine_id:'WAUD-001',context:c,url:u.toString(),checks:['availability','http_headers','robots_sitemap','indexability','metadata','structured_data','internal_links','performance_hints','accessibility_hints','security_headers','seo_basics'],outputs:['findings','severity','evidence_refs','recommended_actions'],mode:'READ_ONLY_PUBLIC_AUDIT',executed:false,prod_writes:false,trading_access:false,additional_cost_target_eur:0});}
