const REQUIRED_CONTEXT=['company_id','engine_id','environment','version'];
function nonEmpty(v,l){if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim();}
function ctx(input){const c=input?.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of REQUIRED_CONTEXT)nonEmpty(c[k],`context.${k}`);if(c.engine_id!=='SOCAUD-001')throw new Error('engine_id must be SOCAUD-001');if(c.environment!=='SCAFFOLD')throw new Error('environment must be SCAFFOLD');return structuredClone(c);}
function human(reason,c){return Object.freeze({status:'HUMAN_REQUIRED',reason,context:c,executed:false});}
export function planSocialMediaAudit(input){const c=ctx(input);if(input?.authorized!==true)return human('POLICY_CONFLICT',c);if(input?.requires_prod_write===true)return human('HIGH_RISK',c);const cost=Number(input?.estimated_additional_cost_eur??0);if(!Number.isFinite(cost)||cost<0)throw new Error('invalid cost');if(cost>0)return human('MONEY_LIMIT',c);const handles=[...(input?.profiles??[])].map((x,i)=>nonEmpty(x,`profiles[${i}]`));if(handles.length===0)throw new Error('profiles required');return Object.freeze({status:'PLAN_READY',engine_id:'SOCAUD-001',context:c,profiles:[...new Set(handles)].sort(),checks:['profile_presence','branding_consistency','bio_links','posting_recency','content_mix','engagement_signals','cta_consistency','cross_platform_consistency'],outputs:['findings','severity','evidence_refs','recommended_actions'],mode:'READ_ONLY_PUBLIC_AUDIT',executed:false,prod_writes:false,trading_access:false,additional_cost_target_eur:0});}
