const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
function stableContext(value){if(!value||typeof value!=='object'||Array.isArray(value))throw new Error('context required');const d=Object.getOwnPropertyDescriptors(value);const out={};for(const k of ['company_id','engine_id','environment','version']){const x=d[k];if(!x||!Object.prototype.hasOwnProperty.call(x,'value')||x.get||x.set)throw new Error(`context.${k} must be a stable data property`);out[k]=req(x.value,`context.${k}`)}return Object.freeze(out)}
function strictBoolean(v,l){if(typeof v!=='boolean')throw new Error(`${l} must be boolean`);return v}
function strictCost(i){if(!Object.prototype.hasOwnProperty.call(i,'estimated_additional_cost_eur'))return 0;const v=i.estimated_additional_cost_eur;if(typeof v!=='number'||!Number.isFinite(v)||v<0)throw new Error('estimated_additional_cost_eur must be a finite non-negative number');return v}
export function planCompanyHealth(i={}){if(!i||typeof i!=='object'||Array.isArray(i))throw new Error('input required');const c=stableContext(i.context);if(c.engine_id!=='COMP-HLT-001')throw new Error('engine_id must be COMP-HLT-001');if(c.environment!=='SCAFFOLD')throw new Error('environment must be SCAFFOLD');const authorized=strictBoolean(i.authorized,'authorized');const requiresProdWrite=Object.prototype.hasOwnProperty.call(i,'requires_prod_write')?strictBoolean(i.requires_prod_write,'requires_prod_write'):false;if(!authorized)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',context:c,executed:false});if(requiresProdWrite)return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK',context:c,executed:false});const cost=strictCost(i);if(cost>0)return Object.freeze({status:'HUMAN_REQUIRED',reason:'MONEY_LIMIT',context:c,executed:false});return Object.freeze({status:'PLAN_READY',engine_id:'COMP-HLT-001',context:c,checks:Object.freeze(['engine_health','dependency_health','job_health','queue_health','error_rate','latency','cost_guardrail','backup_freshness','rollback_readiness','human_exception_rate']),mode:'READ_ONLY_HEALTH_PLAN',prod_writes:false,executed:false,additional_cost_eur:0})}
