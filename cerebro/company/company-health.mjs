const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function planCompanyHealth(i){const c=i?.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','engine_id','environment','version'])req(c[k],`context.${k}`);if(c.engine_id!=='COMP-HLT-001')throw new Error('engine_id must be COMP-HLT-001');if(c.environment!=='SCAFFOLD')throw new Error('environment must be SCAFFOLD');if(i?.authorized!==true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',context:structuredClone(c),executed:false});return Object.freeze({status:'PLAN_READY',engine_id:'COMP-HLT-001',context:structuredClone(c),checks:['engine_health','dependency_health','job_health','queue_health','error_rate','latency','cost_guardrail','backup_freshness','rollback_readiness','human_exception_rate'],mode:'READ_ONLY_HEALTH_PLAN',prod_writes:false,executed:false})}
