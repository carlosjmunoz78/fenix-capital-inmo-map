const REQUIRED_CONTEXT=['company_id','engine_id','environment','version'];
const HUMAN_REQUIRED=new Set(['LEGAL_REQUIRED','SIGNATURE_REQUIRED','LOW_CONFIDENCE','HIGH_RISK','POLICY_CONFLICT','SECURITY_INCIDENT','MONEY_LIMIT','CUSTOMER_HUMAN_REQUEST']);
function nonEmpty(v,l){if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim();}
function contextOf(input){const c=input?.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of REQUIRED_CONTEXT)nonEmpty(c[k],`context.${k}`);if(c.engine_id!=='BMD-001')throw new Error('engine_id must be BMD-001');if(c.environment!=='SCAFFOLD')throw new Error('environment must be SCAFFOLD');return structuredClone(c);}
function human(reason,ctx){if(!HUMAN_REQUIRED.has(reason))throw new Error('invalid HUMAN_REQUIRED');return Object.freeze({status:'HUMAN_REQUIRED',reason,context:ctx,executed:false});}
export function planBusinessModelDiscovery(input){const ctx=contextOf(input);if(input?.authorized!==true)return human('POLICY_CONFLICT',ctx);if(input?.requires_prod_write===true)return human('HIGH_RISK',ctx);const cost=Number(input?.estimated_additional_cost_eur??0);if(!Number.isFinite(cost)||cost<0)throw new Error('invalid cost');if(cost>0)return human('MONEY_LIMIT',ctx);const evidence=Array.isArray(input?.evidence_refs)?[...new Set(input.evidence_refs.map(x=>nonEmpty(x,'evidence_ref')))]:[];const lenses=['customer_segments','problems_jobs','value_propositions','channels','revenue_streams','cost_structure','key_resources','key_activities','key_partners','unit_economics','risks_assumptions'];return Object.freeze({status:'PLAN_READY',engine_id:'BMD-001',context:ctx,lenses,evidence_refs:evidence,outputs:['business_model_hypotheses','assumption_register','evidence_gaps','priority_questions'],mode:'READ_ONLY_ANALYSIS',executed:false,prod_writes:false,trading_access:false,additional_cost_target_eur:0});}
