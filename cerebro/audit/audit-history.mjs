const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function buildAuditRecord(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','environment','version'])req(c[k],`context.${k}`);for(const k of ['request_id','actor_id','action','decision','evidence_ref'])req(i[k],k);if(i.secret_payload_included===true)return Object.freeze({status:'BLOCKED',reason:'SECURITY_INCIDENT',persist:false});return Object.freeze({status:'AUDIT_RECORD_READY',persist:true,record:Object.freeze({company_id:c.company_id,environment:c.environment,version:c.version,request_id:i.request_id,actor_id:i.actor_id,action:i.action,decision:i.decision,evidence_ref:i.evidence_ref,created_at:i.created_at??null,secret_payload_included:false}),append_only:true,mutable:false})}
