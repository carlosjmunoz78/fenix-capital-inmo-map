const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function evaluateFranchiseReplication(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','engine_id','environment','version'])req(c[k],`context.${k}`);if(c.engine_id!=='FRAN-001')throw new Error('engine_id must be FRAN-001');if(!['SCAFFOLD','LAB','PREPROD'].includes(c.environment))return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK',ready:false});if(i.trading===true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',ready:false});const territory=req(i.territory,'territory');const minima=Array.isArray(i.minimum_requirements)?i.minimum_requirements:[];if(!minima.length)return Object.freeze({status:'BLOCKED',reason:'POLICY_CONFLICT',ready:false,issue:'MINIMUM_REQUIREMENTS_REQUIRED'});const config=i.config&&typeof i.config==='object'&&!Array.isArray(i.config)?i.config:null;if(!config)return Object.freeze({status:'BLOCKED',reason:'POLICY_CONFLICT',ready:false,issue:'CONFIG_REQUIRED'});if(i.legal_approval_required===true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'LEGAL_REQUIRED',ready:false});return Object.freeze({status:'REPLICATION_TEMPLATE_READY',ready:true,territory,minimum_requirements:Object.freeze([...minima]),config:Object.freeze({...config}),factory_required:true,rebuild_required:true,expansion_decision_execute:false,deploy_execute:false,read_only:true,plan_only:true,executed:false,additional_cost_eur:0});}