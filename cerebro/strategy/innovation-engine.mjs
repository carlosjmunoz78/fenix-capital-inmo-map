const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function evaluateInnovation(i={}){const c=i.context;if(!c||typeof c!=='object')throw new Error('context required');for(const k of ['company_id','engine_id','environment','version'])req(c[k],`context.${k}`);if(!['LAB','PREPROD'].includes(c.environment))return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK',proposal_execute:false});if(c.engine_id!=='INN-001')return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',proposal_execute:false});const confidence=Number(i.confidence);if(!Number.isFinite(confidence)||confidence<0.7)return Object.freeze({status:'HUMAN_REQUIRED',reason:'LOW_CONFIDENCE',proposal_execute:false});const ideas=Array.isArray(i.ideas)?i.ideas:[];const ranked=ideas.map((x,idx)=>({id:req(x.id,`ideas[${idx}].id`),impact:Number(x.impact)||0,effort:Number(x.effort)||0,risk:Number(x.risk)||0,evidence_refs:Array.isArray(x.evidence_refs)?x.evidence_refs:[],score:(Number(x.impact)||0)*4-(Number(x.effort)||0)*2-(Number(x.risk)||0)*3})).sort((a,b)=>b.score-a.score||a.id.localeCompare(b.id));if(ranked.some(x=>x.risk>=5))return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK',proposal_execute:false,ranked:Object.freeze(ranked)});return Object.freeze({status:'PROPOSALS_READY',ranked:Object.freeze(ranked),proposal_execute:false,read_only:true,plan_only:true,executed:false,additional_cost_eur:0})}
