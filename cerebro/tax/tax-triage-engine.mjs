export function assessTaxCase(input={}){const {context={},authorized=false,requires_prod_write=false,confidence=0,source_refs=[],facts={}}=input;if(!context.company_id||context.engine_id!=='TAX-001'||!['LAB','PREPROD'].includes(context.environment))throw new Error('UNSAFE_CONTEXT');if(!Array.isArray(source_refs)||!source_refs.length)throw new Error('EVIDENCE_REQUIRED');if(!authorized)return human('POLICY_CONFLICT');if(requires_prod_write)return human('HIGH_RISK');if(confidence<.8)return human('LOW_CONFIDENCE');const complexity=Number(facts.complexity??0);if(facts.requires_filing===true||facts.requires_interpretation===true||complexity>=4)return human('LEGAL_REQUIRED');return {status:'TAX_TRIAGE_READY',engine_id:'TAX-001',company_id:context.company_id,tax_decision:false,next_action:'PREPARE_TAX_PACKET',read_only:true,plan_only:true,executed:false,prod_writes:false,cost_additional_eur:0,source_refs:[...source_refs]}}function human(reason){return {status:'HUMAN_REQUIRED',reason,read_only:true,plan_only:true,executed:false,prod_writes:false}}