const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function planCommandV0(i){const c=i?.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','engine_id','environment','version'])req(c[k],`context.${k}`);if(c.engine_id!=='CMD-001')throw new Error('engine_id must be CMD-001');if(c.environment!=='SCAFFOLD')throw new Error('environment must be SCAFFOLD');if(i?.authorized!==true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',executed:false,context:structuredClone(c)});const command=req(i?.command,'command');const risk=req(i?.risk??'LOW','risk');if(!['LOW','MEDIUM','HIGH'].includes(risk))throw new Error('invalid risk');if(risk==='HIGH')return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK',executed:false,context:structuredClone(c)});const cost=Number(i?.estimated_additional_cost_eur??0);if(!Number.isFinite(cost)||cost<0)throw new Error('invalid cost');if(cost>0)return Object.freeze({status:'HUMAN_REQUIRED',reason:'MONEY_LIMIT',executed:false,context:structuredClone(c)});return Object.freeze({status:'COMMAND_PLANNED',engine_id:'CMD-001',context:structuredClone(c),command,risk,gateway_required:true,audit_required:true,idempotency_key_required:true,mode:'PLAN_ONLY',prod_writes:false,executed:false})}
