const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function buildTimeline(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','environment','version'])req(c[k],`context.${k}`);if(c.environment!=='PREPROD')return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK'});const events=Array.isArray(i.events)?i.events:[];const out=[];for(let n=0;n<events.length;n++){const e=events[n];if(!e||typeof e!=='object'||Array.isArray(e))throw new Error(`events[${n}] required`);for(const k of ['event_id','company_id','timestamp','type','evidence_ref'])req(e[k],`events[${n}].${k}`);if(e.company_id!==c.company_id)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT'});if(e.secret_payload===true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'SECURITY_INCIDENT'});const t=Date.parse(e.timestamp);if(!Number.isFinite(t))throw new Error(`events[${n}].timestamp invalid`);out.push(Object.freeze({event_id:e.event_id,timestamp:new Date(t).toISOString(),type:e.type,evidence_ref:e.evidence_ref}))}out.sort((a,b)=>a.timestamp.localeCompare(b.timestamp)||a.event_id.localeCompare(b.event_id));return Object.freeze({status:'READY',company_id:c.company_id,events:Object.freeze(out),read_only:true,audit_required:true})}
