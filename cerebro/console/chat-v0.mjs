const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
const deepFreeze=o=>{if(o&&typeof o==='object'&&!Object.isFrozen(o)){for(const v of Object.values(o))deepFreeze(v);Object.freeze(o)}return o};
function ctxOf(i){const raw=i?.context;if(!raw||Object.getPrototypeOf(raw)!==Object.prototype)throw new Error('context plain object required');for(const d of Object.values(Object.getOwnPropertyDescriptors(raw)))if('get'in d||'set'in d)throw new Error('context accessors forbidden');const c=structuredClone(raw);for(const k of ['company_id','engine_id','environment','version'])req(c[k],`context.${k}`);if(c.engine_id!=='CHAT-001')throw new Error('engine_id must be CHAT-001');if(c.environment!=='PREPROD')throw new Error('CHAT-001 gateway adapter requires PREPROD');return deepFreeze(c)}
function costOf(i){if(i?.estimated_additional_cost_eur===undefined)return 0;const v=i.estimated_additional_cost_eur;if(typeof v!=='number'||!Number.isFinite(v)||v<0)throw new Error('invalid cost');return v}
export function buildChatV0(i){const c=ctxOf(i);const message=req(i?.message,'message');const cost=costOf(i);if(cost>0)return deepFreeze({status:'HUMAN_REQUIRED',reason:'MONEY_LIMIT',executed:false,context:c});return deepFreeze({status:'PLAN_READY',engine_id:'CHAT-001',context:c,message,gateway_route:'/cerebro/gateway/chat',gateway_adapter_contract:true,model_router_required:true,direct_model_access:false,audit_required:true,history_required:true,mode:'PLAN_ONLY',prod_writes:false,trading_access:false,autonomous_prod:false,executed:false,additional_cost_target_eur:0})}
