const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function planVoiceUi(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','environment','version'])req(c[k],`context.${k}`);if(c.environment!=='PREPROD')return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK'});if(i.recording===true&&i.consent!==true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT'});if(i.direct_model_access===true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT'});if(i.additional_cost_eur!=null&&Number(i.additional_cost_eur)>0)return Object.freeze({status:'HUMAN_REQUIRED',reason:'MONEY_LIMIT'});return Object.freeze({status:'READY',company_id:c.company_id,gateway_required:true,direct_model_access:false,recording_allowed:i.recording===true&&i.consent===true,transcript_audit_required:true,additional_cost_eur:0})}
