const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function explainDecision(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','engine_id','environment','version'])req(c[k],`context.${k}`);for(const k of ['request_id','decision','evidence_ref'])req(i[k],k);if(i.secret_payload===true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'SECURITY_INCIDENT'});return Object.freeze({status:'EXPLAINED',company_id:c.company_id,engine_id:c.engine_id,request_id:i.request_id,decision:i.decision,evidence_ref:i.evidence_ref,why:Array.isArray(i.reasons)?Object.freeze(i.reasons.map(String)):Object.freeze([]),model_required:false,audit_required:true})}
