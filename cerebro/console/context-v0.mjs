const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function resolveContextV0(i){const c=i?.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','engine_id','environment','version'])req(c[k],`context.${k}`);if(c.engine_id!=='CTX-001')throw new Error('engine_id must be CTX-001');if(c.environment!=='SCAFFOLD')throw new Error('environment must be SCAFFOLD');if(i?.authorized!==true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',executed:false,context:structuredClone(c)});const scope=req(i?.scope,'scope');const allowed=['company','engine','process','customer','document','training','global_readonly'];if(!allowed.includes(scope))return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',executed:false,context:structuredClone(c)});return Object.freeze({status:'CONTEXT_READY',engine_id:'CTX-001',company_id:c.company_id,scope,company_boundary_enforced:true,cross_company:false,audit_required:true,mode:'READ_ONLY_CONTEXT',executed:false})}
