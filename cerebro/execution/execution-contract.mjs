const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function buildExecutionContract(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','identity_id','account_id','engine_id','environment','version'])req(c[k],`context.${k}`);for(const k of ['request_id','connector_id','action','policy_result','evidence_ref'])req(i[k],k);const cost=Number(i.cost_eur??0);if(!Number.isFinite(cost)||cost<0)throw new Error('invalid cost');if(cost>0&&i.money_authorized!==true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'MONEY_LIMIT',ready:false});if(i.secret_payload_included===true)return Object.freeze({status:'BLOCKED',reason:'SECURITY_INCIDENT',ready:false});return Object.freeze({status:'EXECUTION_CONTRACT_READY',ready:true,execution:Object.freeze({request_id:i.request_id,company_id:c.company_id,identity_id:c.identity_id,account_id:c.account_id,engine_id:c.engine_id,connector_id:i.connector_id,action:i.action,policy_result:i.policy_result,result:i.result??null,evidence_ref:i.evidence_ref,cost_eur:cost,timestamp:i.timestamp??null}),verify_required:true,audit_required:true,secret_material:false})}
