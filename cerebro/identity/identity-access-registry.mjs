const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
const LOGIN=new Set(['email_password','google','microsoft','apple','social','passkey','oauth','sso','session','api_key_ref','other']);
export function registerIdentityAccount(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','environment','version'])req(c[k],`context.${k}`);for(const k of ['identity_id','account_id','owner_type','owner_id','provider','login_method','status'])req(i[k],k);if(!LOGIN.has(i.login_method))throw new Error('unsupported login_method');if(i.secret_value!==undefined)return Object.freeze({status:'BLOCKED',reason:'SECURITY_INCIDENT',registered:false});return Object.freeze({status:'REGISTERED',registered:true,identity:Object.freeze({identity_id:i.identity_id,owner_type:i.owner_type,owner_id:i.owner_id,company_id:c.company_id,status:i.identity_status??'ACTIVE',policy_set:i.policy_set??'default'}),account:Object.freeze({account_id:i.account_id,identity_id:i.identity_id,provider:i.provider,handle_or_email:i.handle_or_email??null,login_method:i.login_method,purpose:i.purpose??null,environment:c.environment,status:i.status,verified_at:i.verified_at??null}),secret_stored:false,audit_required:true})}
