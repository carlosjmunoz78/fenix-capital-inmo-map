const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function resolveCredentialRef(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','identity_id','account_id','environment','version'])req(c[k],`context.${k}`);for(const k of ['credential_ref_id','vault_provider','secret_ref','rotation_policy'])req(i[k],k);if(i.secret_value!==undefined||i.raw_secret!==undefined)return Object.freeze({status:'BLOCKED',reason:'SECURITY_INCIDENT',credential_available:false});if(i.revoked===true||i.expired===true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',credential_available:false,renewal_required:true});const scopes=Array.isArray(i.scopes)?i.scopes.filter(Boolean):[];return Object.freeze({status:'CREDENTIAL_REF_READY',credential_available:true,credential_ref:Object.freeze({credential_ref_id:i.credential_ref_id,account_id:c.account_id,vault_provider:i.vault_provider,secret_ref:i.secret_ref,scopes:Object.freeze(scopes),expires_at:i.expires_at??null,rotation_policy:i.rotation_policy}),secret_material_returned:false,audit_required:true})}
