const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function discoverSessionMetadata(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','identity_id','account_id','environment','version'])req(c[k],`context.${k}`);for(const k of ['provider','login_method','session_status'])req(i[k],k);if(i.secret_payload!==undefined||i.password!==undefined||i.token_value!==undefined)return Object.freeze({status:'BLOCKED',reason:'SECURITY_INCIDENT',recorded:false});const allowed=new Set(['ACTIVE','EXPIRED','REVOKED','UNKNOWN']);if(!allowed.has(i.session_status))throw new Error('unsupported session_status');if(i.session_status==='EXPIRED'||i.session_status==='REVOKED')return Object.freeze({status:'SESSION_RENEWAL_REQUIRED',reason:'POLICY_CONFLICT',recorded:true,session_status:i.session_status,renewal_or_fallback_required:true,secret_material:false});return Object.freeze({status:'SESSION_METADATA_READY',recorded:true,session_status:i.session_status,provider:i.provider,login_method:i.login_method,scope:i.scope??null,verified_at:i.verified_at??null,secret_material:false,audit_required:true})}
