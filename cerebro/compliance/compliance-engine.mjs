export function assessCompliance(input={}){const {context={},authorized=false,requires_prod_write=false,confidence=0,source_refs=[],controls=[]}=input;if(!context.company_id||context.engine_id!=='CMP-002'||!['LAB','PREPROD'].includes(context.environment))throw new Error('UNSAFE_CONTEXT');if(!Array.isArray(source_refs)||!source_refs.length)throw new Error('EVIDENCE_REQUIRED');if(!authorized)return human('POLICY_CONFLICT');if(requires_prod_write)return human('HIGH_RISK');if(confidence<.8)return human('LOW_CONFIDENCE');if(!Array.isArray(controls))throw new Error('CONTROLS_REQUIRED');const failed=controls.filter(c=>c.required===true&&c.passed!==true);if(failed.some(c=>c.legal===true))return human('LEGAL_REQUIRED');if(failed.some(c=>Number(c.severity??1)>=4))return human('HIGH_RISK');return {status:failed.length?'COMPLIANCE_REMEDIATION_PLAN':'COMPLIANCE_CHECK_CLEAR',engine_id:'CMP-002',company_id:context.company_id,failed_controls:failed.map(c=>c.id),compliance_decision:false,read_only:true,plan_only:true,executed:false,prod_writes:false,cost_additional_eur:0,source_refs:[...source_refs]}}function human(reason){return {status:'HUMAN_REQUIRED',reason,read_only:true,plan_only:true,executed:false,prod_writes:false}}