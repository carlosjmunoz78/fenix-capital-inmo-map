import test from 'node:test';import assert from 'node:assert/strict';import {evaluateOpportunity} from '../strategy/opportunity-engine.mjs';
const context={company_id:'fenix',engine_id:'OPP-001',environment:'PREPROD',version:'0.1.0'};
test('OPP-001 recommends MVP only with good ROI/risk',()=>{const r=evaluateOpportunity({context,authorized:true,confidence:.9,roi_score:80,risk_score:30,mvp_cost_eur:0,preauthorized_budget_eur:0});assert.equal(r.decision,'MVP_RECOMMENDED');assert.equal(r.mvp_execute,false);assert.equal(r.scale_only_after_evidence,true)});
test('OPP-001 requires more research for middling cases',()=>{const r=evaluateOpportunity({context,authorized:true,confidence:.9,roi_score:55,risk_score:40,mvp_cost_eur:0,preauthorized_budget_eur:0});assert.equal(r.decision,'RESEARCH_MORE')});
test('OPP-001 escalates risk, money and low confidence',()=>{assert.equal(evaluateOpportunity({context,authorized:true,confidence:.9,roi_score:90,risk_score:90,mvp_cost_eur:0,preauthorized_budget_eur:0}).reason,'HIGH_RISK');assert.equal(evaluateOpportunity({context,authorized:true,confidence:.9,roi_score:80,risk_score:20,mvp_cost_eur:10,preauthorized_budget_eur:0}).reason,'MONEY_LIMIT');assert.equal(evaluateOpportunity({context,authorized:true,confidence:.4,roi_score:80,risk_score:20}).reason,'LOW_CONFIDENCE')});
