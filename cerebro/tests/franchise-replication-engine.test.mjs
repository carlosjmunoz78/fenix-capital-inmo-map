import test from 'node:test';import assert from 'node:assert/strict';import {evaluateFranchiseReplication} from '../strategy/franchise-replication-engine.mjs';
const context={company_id:'fenix',engine_id:'FRAN-001',environment:'PREPROD',version:'0.1.0'};
test('builds a territory replication template',()=>{const r=evaluateFranchiseReplication({context,territory:'Jaen',minimum_requirements:['brand','banking','seo'],config:{locale:'es-ES'}});assert.equal(r.status,'REPLICATION_TEMPLATE_READY');assert.equal(r.deploy_execute,false)});
test('requires minima and config',()=>{assert.equal(evaluateFranchiseReplication({context,territory:'Jaen',minimum_requirements:[],config:{}}).status,'BLOCKED');assert.equal(evaluateFranchiseReplication({context,territory:'Jaen',minimum_requirements:['x']}).status,'BLOCKED')});
test('escalates legal and unsafe scope',()=>{assert.equal(evaluateFranchiseReplication({context,territory:'Jaen',minimum_requirements:['x'],config:{},legal_approval_required:true}).reason,'LEGAL_REQUIRED');assert.equal(evaluateFranchiseReplication({context:{...context,environment:'PROD'},territory:'Jaen',minimum_requirements:['x'],config:{}}).reason,'HIGH_RISK');assert.equal(evaluateFranchiseReplication({context,territory:'Jaen',minimum_requirements:['x'],config:{},trading:true}).reason,'POLICY_CONFLICT')});