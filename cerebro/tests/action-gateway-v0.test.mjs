import test from 'node:test';import assert from 'node:assert/strict';import {planActionGatewayV0} from '../gateway/action-gateway-v0.mjs';
const base={context:{company_id:'fenix',engine_id:'ACTGW-001',environment:'SCAFFOLD',version:'0.1.0'},authorized:true,action:'query_status',target:'engine:FACT-001',risk:'LOW',signature_required:false,estimated_additional_cost_eur:0};
test('ACTGW plans guarded action',()=>{const r=planActionGatewayV0(base);assert.equal(r.status,'ACTION_PLANNED');assert.equal(r.permission_check_required,true);assert.equal(r.rollback_required,true)});
test('ACTGW blocks high risk signature and cost',()=>{assert.equal(planActionGatewayV0({...base,risk:'HIGH'}).reason,'HIGH_RISK');assert.equal(planActionGatewayV0({...base,signature_required:true}).reason,'SIGNATURE_REQUIRED');assert.equal(planActionGatewayV0({...base,estimated_additional_cost_eur:1}).reason,'MONEY_LIMIT')});
