import test from 'node:test';import assert from 'node:assert/strict';import {evaluateAiLab} from '../labs/ai-lab.mjs';
const context={company_id:'fenix',engine_id:'LAB-AI',environment:'LAB',version:'0.1.0'};
test('LAB-AI validates synthetic zero-cost model experiment',()=>{const r=evaluateAiLab({context,candidate:{confidence:.9,estimated_cost_eur:0,kind:'MODEL',synthetic_dataset:true}});assert.equal(r.status,'LAB_CANDIDATE_VALIDATED');assert.equal(r.model_integration_execute,false);assert.equal(r.paid_ai_required,false)});
test('LAB-AI blocks secrets and non-synthetic datasets',()=>{assert.equal(evaluateAiLab({context,candidate:{confidence:.9,kind:'PROMPT',synthetic_dataset:true,uses_real_secrets:true}}).reason,'SECURITY_INCIDENT');assert.equal(evaluateAiLab({context,candidate:{confidence:.9,kind:'MODEL',synthetic_dataset:false}}).reason,'POLICY_CONFLICT')});
test('LAB-AI blocks paid and unknown candidates',()=>{assert.equal(evaluateAiLab({context,candidate:{confidence:.9,estimated_cost_eur:1,kind:'MODEL',synthetic_dataset:true}}).reason,'MONEY_LIMIT');assert.equal(evaluateAiLab({context,candidate:{confidence:.9,kind:'UNKNOWN',synthetic_dataset:true}}).reason,'POLICY_CONFLICT')});
