import test from 'node:test';import assert from 'node:assert/strict';import {evaluateAutomationLab} from '../labs/automation-lab.mjs';
const context={company_id:'fenix',engine_id:'LAB-AUT',environment:'LAB',version:'0.1.0'};
test('LAB-AUT validates synthetic workflow failure-mode test',()=>{const r=evaluateAutomationLab({context,candidate:{confidence:.9,estimated_cost_eur:0,trigger:'EVENT',synthetic_fixture:true,failure_mode:'RETRY'}});assert.equal(r.status,'LAB_CANDIDATE_VALIDATED');assert.equal(r.workflow_execute,false);assert.equal(r.prod_writes,false)});
test('LAB-AUT requires synthetic fixtures and allowlisted failure modes',()=>{assert.equal(evaluateAutomationLab({context,candidate:{confidence:.9,trigger:'EVENT',synthetic_fixture:false}}).reason,'POLICY_CONFLICT');assert.equal(evaluateAutomationLab({context,candidate:{confidence:.9,trigger:'EVENT',synthetic_fixture:true,failure_mode:'CHAOS'}}).reason,'POLICY_CONFLICT')});
test('LAB-AUT gates cost and confidence',()=>{assert.equal(evaluateAutomationLab({context,candidate:{confidence:.9,estimated_cost_eur:1,trigger:'EVENT',synthetic_fixture:true}}).reason,'MONEY_LIMIT');assert.equal(evaluateAutomationLab({context,candidate:{confidence:.2,trigger:'EVENT',synthetic_fixture:true}}).reason,'LOW_CONFIDENCE')});
