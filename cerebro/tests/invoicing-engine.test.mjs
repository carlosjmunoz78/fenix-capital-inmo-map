import test from 'node:test';import assert from 'node:assert/strict';import {planInvoice} from '../finance/invoicing-engine.mjs';const b={context:{company_id:'fenix',engine_id:'INV-001',environment:'PREPROD'},authorized:true,confidence:.95,source_refs:['ev']};test('tax review gate',()=>assert.equal(planInvoice({...b,invoice:{net_cents:10000,vat_cents:2100,tax_reviewed:false}}).reason,'LEGAL_REQUIRED'));test('ready',()=>{const r=planInvoice({...b,invoice:{invoice_ref:'i1',net_cents:10000,vat_cents:2100,tax_reviewed:true}});assert.equal(r.total_cents,12100);assert.equal(r.issue_execute,false)});test('integer cents',()=>assert.throws(()=>planInvoice({...b,invoice:{net_cents:10.5,vat_cents:2,tax_reviewed:true}}),/INTEGER_CENTS_REQUIRED/));