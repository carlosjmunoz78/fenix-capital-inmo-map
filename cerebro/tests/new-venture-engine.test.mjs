import test from 'node:test';import assert from 'node:assert/strict';import {evaluateNewVenture} from '../strategy/new-venture-engine.mjs';
const context={company_id:'fenix',engine_id:'VENT-001',environment:'PREPROD',version:'0.1.0'};
test('recommends MVP only with strong research and economics',()=>{const r=evaluateNewVenture({context,idea:'service',research_score:85,unit_economics_score:85,risk_score:30,confidence:.9});assert.equal(r.decision,'MVP_PLAN_READY');assert.equal(r.mvp_execute,false)});
test('fails closed on confidence risk and budget',()=>{assert.equal(evaluateNewVenture({context,idea:'x',research_score:70,unit_economics_score:70,risk_score:30,confidence:.5}).reason,'LOW_CONFIDENCE');assert.equal(evaluateNewVenture({context,idea:'x',research_score:90,unit_economics_score:90,risk_score:90,confidence:.9}).reason,'HIGH_RISK');assert.equal(evaluateNewVenture({context,idea:'x',research_score:90,unit_economics_score:90,risk_score:20,confidence:.9,mvp_budget_eur:10}).reason,'MONEY_LIMIT')});
test('blocks PROD and Trading',()=>{assert.equal(evaluateNewVenture({context:{...context,environment:'PROD'},idea:'x',research_score:80,unit_economics_score:80,risk_score:20,confidence:.9}).reason,'HIGH_RISK');assert.equal(evaluateNewVenture({context,idea:'x',research_score:80,unit_economics_score:80,risk_score:20,confidence:.9,trading:true}).reason,'POLICY_CONFLICT')});