const req=(v,l)=>{if(typeof v!=='string'||!v.trim())throw new Error(`${l} required`);return v.trim()};
export function evaluateResilienceGate(i={}){const c=i.context;if(!c||typeof c!=='object'||Array.isArray(c))throw new Error('context required');for(const k of ['company_id','engine_id','environment','version'])req(c[k],`context.${k}`);if(c.environment!=='PREPROD')return Object.freeze({status:'HUMAN_REQUIRED',reason:'HIGH_RISK',ready:false});if(i.trading===true)return Object.freeze({status:'HUMAN_REQUIRED',reason:'POLICY_CONFLICT',ready:false});if(i.additional_cost_eur!=null&&Number(i.additional_cost_eur)>0)return Object.freeze({status:'HUMAN_REQUIRED',reason:'MONEY_LIMIT',ready:false});const gates={backup_snapshot:i.backup_snapshot===true,restore_test:i.restore_test===true,rollback_test:i.rollback_test===true,rebuild_test:i.rebuild_test===true,dependency_map:i.dependency_map===true,current_contract:i.current_contract===true,observability:i.observability===true,audit:i.audit===true};const missing=Object.entries(gates).filter(([,v])=>!v).map(([k])=>k);if(missing.length)return Object.freeze({status:'BLOCKED',reason:'POLICY_CONFLICT',ready:false,missing:Object.freeze(missing)});return Object.freeze({status:'RESILIENCE_READY',ready:true,company_id:c.company_id,engine_id:c.engine_id,dr_ready:true,rebuild_ready:true,bcp_ready:true,prod_promotion:false,audit_required:true,gates:Object.freeze(gates)})}
