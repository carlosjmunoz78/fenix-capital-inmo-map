import {createClient} from 'https://esm.sh/@supabase/supabase-js@2';
const ALLOWED=new Set(['https://app.fenixcapital.es']);
function key(raw:string){try{const x=JSON.parse(raw||'{}');return String(x?.default??Object.values(x??{})[0]??'')}catch{return''}}
function cfg(){return{url:Deno.env.get('SUPABASE_URL')??'',anon:Deno.env.get('SUPABASE_ANON_KEY')||key(Deno.env.get('SUPABASE_PUBLISHABLE_KEYS')??''),service:Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')||key(Deno.env.get('SUPABASE_SECRET_KEYS')??'')}}
function cors(req:Request){const o=req.headers.get('origin')||'';return{'Access-Control-Allow-Origin':ALLOWED.has(o)?o:'https://app.fenixcapital.es','Access-Control-Allow-Headers':'authorization,apikey,content-type','Access-Control-Allow-Methods':'POST,OPTIONS','Vary':'Origin','x-fenix-env':'PROD'}}
function out(req:Request,b:any,s=200){return new Response(JSON.stringify(b),{status:s,headers:{...cors(req),'content-type':'application/json','cache-control':'no-store'}})}
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response(null,{status:204,headers:cors(req)});if(req.method!=='POST')return out(req,{ok:false,error:'method_not_allowed'},405);const c=cfg();if(!c.url||!c.anon||!c.service)return out(req,{ok:false,error:'server_config_missing'},503);const h=req.headers.get('authorization')||'';if(!h.startsWith('Bearer '))return out(req,{ok:false,error:'unauthorized'},401);const auth=createClient(c.url,c.anon,{auth:{persistSession:false,autoRefreshToken:false}});const svc=createClient(c.url,c.service,{auth:{persistSession:false,autoRefreshToken:false}});const{data:u,error:ue}=await auth.auth.getUser(h.slice(7));if(ue||!u.user)return out(req,{ok:false,error:'unauthorized'},401);const{data:ctx,error:ce}=await svc.rpc('fenix_prod_actor_context_by_auth_server',{p_auth_user_id:u.user.id});if(ce||!ctx?.ok)return out(req,{ok:false,error:'identity_not_linked'},403);let b:any={};try{b=await req.json()}catch{return out(req,{ok:false,error:'invalid_json'},400)}const{data:r,error}=await svc.rpc('fenix_prod_exp_stage_server',{p_actor_code:ctx.actor_code,p_exp_code:String(b.expediente_code||''),p_expected_version:Number(b.expected_version),p_new_stage:String(b.stage||'')});if(error)return out(req,{ok:false,error:'backend_error'},500);return out(req,r,Number(r?.status)||200)});