name: TEMP WAUD-001 Sync

on:
  push:
    branches:
      - cerebro/waud-001-runtime-v0
    paths:
      - '.github/workflows/temp-waud-001-sync.yml'
      - 'cerebro/factory/scripts/temp_waud_sync.py'

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
        with:
          ref: cerebro/waud-001-runtime-v0
          fetch-depth: 0
      - name: Synchronize governance
        run: python cerebro/factory/scripts/temp_waud_sync.py
      - name: Remove temporary promotion artifacts and commit
        shell: bash
        run: |
          set -euo pipefail
          rm -f cerebro/factory/candidates/WAUD-001-0.2.0.json .github/workflows/temp-waud-001-promote.yml
          git config user.name 'CEREBRO Governance'
          git config user.email 'cerebro-governance@users.noreply.github.com'
          git add -A
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'GOV-001 · synchronize WAUD-001 V0.2 contracts and runbook'
          git push origin HEAD:cerebro/waud-001-runtime-v0
