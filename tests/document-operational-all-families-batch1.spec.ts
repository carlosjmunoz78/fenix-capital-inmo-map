import {expect,test} from '@playwright/test';
import {getDocumentOperationalFields} from '../src/documentOperationalFieldProfile';
const m=(row:Record<string,unknown>)=>Object.fromEntries(getDocumentOperationalFields(row).map(x=>[x.label,x.value]));
test('contrato laboral muestra antigüedad contractual y situación',()=>{const x=m({tipo:'Contrato de trabajo',fecha_inicio:'2023-01-10',tipo_contrato:'Indefinido',jornada:'Completa'});expect(String(x['Antigüedad contractual'])).toMatch(/años/);expect(x['Situación contractual']).toContain('Indefinido');});
test('certificado ingresos convierte anual a referencia mensual',()=>{const x=m({tipo:'Certificado de empresa / ingresos',ingresos_totales:36000,retenciones:5400});expect(x['Ingreso mensual de referencia']).toContain('3.000,00');expect(x['Retención efectiva']).toBe('15,0 %');});
test('CIRBE pone riesgo dispuesto y disponible en primer plano',()=>{const x=m({tipo:'CIRBE',riesgo_dispuesto:18500,riesgo_disponible:2500,entidades:'Banco A | Banco B'});expect(x['Riesgo total informado']).toContain('21.000,00');expect(x['Entidades declarantes']).toContain('Banco A');});
test('certificado deuda expone saldo y carga',()=>{const x=m({tipo:'Certificado de deuda',saldo_pendiente:12345.67,cuota:310.2,fecha_cancelacion_prevista:'2029-05-01'});expect(x['Saldo pendiente']).toContain('12.345,67');expect(x['Carga mensual']).toContain('310,20');});
test('arras calcula porcentaje entregado y pendiente',()=>{const x=m({tipo:'Arras / reserva',precio_compraventa:86000,importe_arras:3000});expect(x['Porcentaje ya entregado']).toBe('3,5 %');expect(x['Pendiente hasta compraventa']).toContain('83.000,00');});
test('catastro resume superficies y antigüedad del inmueble',()=>{const x=m({tipo:'Catastro',superficie_construida:106,superficie_parcela:83,anio_construccion:1975});expect(x['Superficie construida']).toBe('106 m²');expect(String(x['Antigüedad aproximada inmueble'])).toMatch(/años/);});
test('oferta bancaria pone cuota financiación y vinculaciones',()=>{const x=m({tipo:'Oferta bancaria',importe_prestamo:180000,precio_compra:200000,cuota:720,vinculaciones:'Nómina + seguro hogar'});expect(x['Financiación sobre precio']).toBe('90,0 %');expect(x['Cuota propuesta']).toContain('720,00');});
test('autónomo muestra años de actividad desde alta',()=>{const x=m({tipo:'Autónomo / alta RETA-IAE',fecha_alta:'2020-03-01',situacion:'Alta'});expect(String(x['Antigüedad como autónomo'])).toMatch(/años/);expect(x['Situación RETA / IAE']).toBe('Alta');});
test('modelo 303 muestra resultado y peso IVA soportado',()=>{const x=m({tipo:'Modelo 303 IVA',iva_repercutido:4200,iva_soportado:1300,resultado:2900});expect(x['Resultado trimestral']).toContain('2.900,00');expect(x['IVA neto calculado']).toContain('2.900,00');});
test('alquiler pone renta mensual y fianza',()=>{const x=m({tipo:'Alquiler',renta:850,fianza:850,duracion:'5 años'});expect(x['Renta mensual']).toContain('850,00');expect(x['Fianza']).toContain('850,00');});
test('IBI pone coste anual y estado de pago',()=>{const x=m({tipo:'IBI',cuota:486.35,ejercicio:'2026',estado_pago:'Pagado'});expect(x['Coste IBI']).toContain('486,35');expect(x['Estado de pago']).toBe('Pagado');});
test('obra nueva pone coste ejecutado y pendiente',()=>{const x=m({tipo:'Obra nueva',coste_ejecutado:120000,coste_pendiente:30000,estado_ejecucion:'80 %'});expect(x['Coste total previsto por evidencia']).toContain('150.000,00');expect(x['Pendiente de ejecutar']).toContain('30.000,00');});
