import {test,expect} from '@playwright/test';
const fakeSession={access_token:'eyJhbGciOiJub25lIiwidHlwIjoiSldUIn0.eyJhdWQiOiJhdXRoZW50aWNhdGVkIiwicm9sZSI6ImF1dGhlbnRpY2F0ZWQiLCJzdWIiOiJhYWFhYWFhYS1hYWFhLTRhYWEtOGFhYS1hYWFhYWFhYWFhYWEiLCJlbWFpbCI6ImRpcmVjY2lvbkBmZW5peC50ZXN0IiwiZXhwIjoxOTk5OTk5OTk5fQ.',token_type:'bearer',expires_in:3600,expires_at:1999999999,refresh_token:'qa-notifications-not-real',user:{id:'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa',aud:'authenticated',role:'authenticated',email:'direccion@fenix.test',app_metadata:{},user_metadata:{},created_at:'2026-08-19T00:00:00.000Z'}};
test.describe('Fénix PRE-PROD · centro de avisos operativos',()=>{test('deriva avisos solo de tareas canónicas pendientes con fecha o criticidad',async({page},testInfo)=>{if(!testInfo.project.name.includes('desktop'))test.skip();const past=new Date(Date.now()-86400000).toISOString().slice(0,10);const future=new Date(Date.now()+86400000*3).toISOString().slice(0,10);await page.setViewportSize({width:1600,height:900});await page.addInitScript(session=>{window.localStorage.setItem('fenix-preprod-auth',JSON.stringify(session));window.localStorage.setItem('fenix-remember-device','true')},fakeSession);await page.route('**/functions/v1/fenix-app-gateway-test/**',async r=>{const u=r.request().url();if(u.endsWith('/session/context'))return r.fulfill({status:200,contentType:'application/json',body:JSON.stringify({actor_code:'DIR-TEST',role:'Direccion'})});if(u.endsWith('/navigation'))return r.fulfill({status:200,contentType:'application/json',body:JSON.stringify({items:[{label:'Inicio',route:'/inicio'},{label:'Agenda',route:'/agenda'}]})});return r.fulfill({status:404,body:'{}'})});await page.route('**/functions/v1/fenix-notion-runtime-test/tareas',r=>r.fulfill({status:200,contentType:'application/json',body:JSON.stringify({items:[{id:'aaaaaaaa111141118111bbbbbbbbbbbb',tarea:'Vencida QA',estado:'Pendiente',fecha_limite:past},{id:'bbbbbbbb111141118111cccccccccccc',tarea:'Crítica QA',estado:'En curso',criticidad:'Crítica',fecha_limite:future},{id:'cccccccc111141118111dddddddddddd',tarea:'Normal sin aviso',estado:'Pendiente'},{id:'dddddddd111141118111eeeeeeeeeeee',tarea:'Terminada',estado:'Completada',fecha_limite:past}]})}));await page.goto('/notificaciones');const root=page.locator('.notif-root');await expect(root).toBeVisible();await expect(root.getByRole('heading',{level:1,name:'Notificaciones',exact:true})).toBeVisible();const kpis=root.locator('.notif-kpis article');await expect(kpis.nth(0).locator('strong')).toHaveText('2');await expect(kpis.nth(1).locator('strong')).toHaveText('1');await expect(kpis.nth(2).locator('strong')).toHaveText('1');await expect(root.getByText('Vencida QA',{exact:true})).toBeVisible();await expect(root.getByText('Crítica QA',{exact:true})).toBeVisible();await expect(root.getByText('Normal sin aviso',{exact:true})).toHaveCount(0);await expect(root.getByText('Terminada',{exact:true})).toHaveCount(0);await expect(root.getByText(/No existe todavía una fuente dedicada de notificaciones/)).toBeVisible();await expect(root.getByText(/\bPRO\b/)).toHaveCount(0);const shot=await root.screenshot();await testInfo.attach('notifications-derived-master-1600',{body:shot,contentType:'image/png'});});test('403 del runtime se presenta como aislamiento sin fabricar avisos',async({page},testInfo)=>{if(!testInfo.project.name.includes('desktop'))test.skip();await page.addInitScript(session=>{window.localStorage.setItem('fenix-preprod-auth',JSON.stringify(session));window.localStorage.setItem('fenix-remember-device','true')},fakeSession);await page.route('**/functions/v1/fenix-app-gateway-test/**',async r=>{const u=r.request().url();if(u.endsWith('/session/context'))return r.fulfill({status:200,contentType:'application/json',body:JSON.stringify({actor_code:'FIN-A',role:'Financiero'})});if(u.endsWith('/navigation'))return r.fulfill({status:200,contentType:'application/json',body:JSON.stringify({items:[{label:'Inicio',route:'/inicio'}]})});return r.fulfill({status:404,body:'{}'})});await page.route('**/functions/v1/fenix-notion-runtime-test/tareas',r=>r.fulfill({status:403,body:'{}'}));await page.goto('/notificaciones');const root=page.locator('.notif-root');await expect(root.getByText('Tu perfil no tiene acceso a estas tareas o avisos derivados.',{exact:true})).toBeVisible();await expect(root.locator('.notif-list>button')).toHaveCount(0);});});
