import {useEffect,useMemo,useState} from 'react';
import {useLocation,useNavigate} from 'react-router-dom';
import {CalendarDays,CheckSquare} from 'lucide-react';
import {fetchAppApi,supabase} from './supabase';
import {fetchNotionRuntime} from './notionRuntime';
import {normalizeNavigation,type NavItem} from './masterNavigation';
import OperationalShellFrame from './OperationalShellFrame';
import {anaVertical} from './assets/visualAssets';
import './operational.css';
import './agenda-polish.css';
import './sortable-table.css';

type Theme='light'|'dark';type SortKey='tarea'|'estado'|'fecha'|'prioridad'|'responsable';type SortDir='asc'|'desc';type Ctx={actor_code?:string;role?:string};type Row=Record<string,unknown>;
const fallbackNav:NavItem[]=[{label:'Inicio',route:'/inicio'}];
function rowsFrom(data:unknown):Row[]{if(!data||typeof data!=='object')return[];const d=data as Record<string,unknown>;return Array.isArray(d.items)?d.items as Row[]:[];}
function text(r:Row,keys:string[]){for(const k of keys){const v=r[k];if(typeof v==='string'&&v.trim())return v.trim();}return'';}
function bool(r:Row,keys:string[]){for(const k of keys){const v=r[k];if(typeof v==='boolean')return v;if(typeof v==='string'&&/^(si|sí|true|1|completada)$/i.test(v.trim()))return true;}return false;}
function idOf(r:Row){return text(r,['id','tarea_id','tarea_code','code']);}
function titleOf(r:Row){return text(r,['tarea','titulo','título','nombre','title'])||'Tarea sin título visible';}
function stateOf(r:Row){return text(r,['estado','status'])||'Sin estado';}
function dueOf(r:Row){return text(r,['fecha_limite','fecha_límite','vencimiento','fecha','due_date'])||'No disponible';}
function priorityOf(r:Row){return text(r,['criticidad','prioridad','priority'])||'No disponible';}
function responsibleOf(r:Row){return text(r,['responsable','trabajador','asignado_a','id_trabajador_operativo'])||'No disponible';}
function completed(r:Row){return bool(r,['completada','completado','done'])||/complet|cerrad|hecha/i.test(stateOf(r));}
function pending(r:Row){return !completed(r);}
function overdue(r:Row){const due=dueOf(r);if(due==='No disponible'||completed(r))return false;const d=new Date(due);if(Number.isNaN(d.getTime()))return /vencid/i.test(stateOf(r));const today=new Date();today.setHours(0,0,0,0);return d.getTime()<today.getTime();}
function compareText(a:string,b:string){return a.localeCompare(b,'es',{sensitivity:'base',numeric:true});}
function sortValue(r:Row,key:SortKey){if(key==='estado')return stateOf(r);if(key==='fecha')return dueOf(r);if(key==='prioridad')return priorityOf(r);if(key==='responsable')return responsibleOf(r);return titleOf(r);}
function compareSortValues(a:string,b:string,dir:SortDir){const missingA=a==='No disponible'||!a,missingB=b==='No disponible'||!b;if(missingA||missingB)return missingA===missingB?0:missingA?1:-1;const da=Date.parse(a),db=Date.parse(b);const base=Number.isFinite(da)&&Number.isFinite(db)?da-db:compareText(a,b);return base*(dir==='asc'?1:-1);}

export default function AgendaShell(){
 const location=useLocation(),navigate=useNavigate();const active=location.pathname==='/agenda';
 const[sessionReady,setSessionReady]=useState(false),[logged,setLogged]=useState(false),[ctx,setCtx]=useState<Ctx|null>(null),[nav,setNav]=useState<NavItem[]>([]),[theme,setTheme]=useState<Theme>(()=>(sessionStorage.getItem('fenix-theme') as Theme)||'light');
 const[rows,setRows]=useState<Row[]>([]),[status,setStatus]=useState<number|null>(null),[loading,setLoading]=useState(false),[message,setMessage]=useState('');
 const[query,setQuery]=useState(''),[state,setState]=useState(''),[priority,setPriority]=useState(''),[selected,setSelected]=useState<string[]>([]),[sortKey,setSortKey]=useState<SortKey>('fecha'),[sortDir,setSortDir]=useState<SortDir>('asc');
 useEffect(()=>{if(!active)return;let alive=true;supabase.auth.getSession().then(({data})=>{if(alive){setLogged(Boolean(data.session));setSessionReady(true)}});const{data:{subscription}}=supabase.auth.onAuthStateChange((_e,s)=>{setLogged(Boolean(s));setSessionReady(true)});return()=>{alive=false;subscription.unsubscribe()};},[active]);
 useEffect(()=>{if(!active)return;document.documentElement.dataset.theme=theme;sessionStorage.setItem('fenix-theme',theme);},[active,theme]);
 useEffect(()=>{if(!active||!logged)return;let alive=true;Promise.all([fetchAppApi<Ctx>('/session/context'),fetchAppApi<unknown>('/navigation')]).then(([c,n])=>{if(!alive)return;setCtx(c.status===200?c.data:null);setNav(n.status===200?normalizeNavigation(n.data):[]);});return()=>{alive=false};},[active,logged]);
 useEffect(()=>{if(!active||!logged)return;let alive=true;(async()=>{setLoading(true);setMessage('');setStatus(null);setRows([]);setSelected([]);try{const r=await fetchNotionRuntime<unknown>('/tareas');if(!alive)return;setStatus(r.status);setRows(r.status===200?rowsFrom(r.data):[]);if(r.status===403)setMessage('Tu perfil no tiene acceso a este módulo o registro.');else if(r.status!==200)setMessage('No se pudo leer la fuente canónica de Agenda/Tareas.');}catch{if(!alive)return;setStatus(0);setRows([]);setSelected([]);setMessage('No se pudo conectar con la fuente canónica de Agenda/Tareas.');}finally{if(alive)setLoading(false);}})();return()=>{alive=false}},[active,logged]);
 const effectiveNav=nav.length?nav:fallbackNav;
 const states=useMemo(()=>Array.from(new Set(rows.map(stateOf).filter(Boolean))).sort(compareText),[rows]);const priorities=useMemo(()=>Array.from(new Set(rows.map(priorityOf).filter(v=>v!=='No disponible'))).sort(compareText),[rows]);
 const visible=useMemo(()=>{const q=query.trim().toLowerCase();const filtered=rows.filter(r=>{const hay=Object.values(r).filter(v=>typeof v==='string'||typeof v==='number').join(' ').toLowerCase();return(!q||hay.includes(q))&&(!state||stateOf(r)===state)&&(!priority||priorityOf(r)===priority)});return[...filtered].sort((a,b)=>{const primary=compareSortValues(sortValue(a,sortKey),sortValue(b,sortKey),sortDir);if(primary!==0)return primary;return compareText(titleOf(a),titleOf(b))*(sortDir==='asc'?1:-1);});},[rows,query,state,priority,sortKey,sortDir]);
 const pendingCount=useMemo(()=>rows.filter(pending).length,[rows]),completedCount=useMemo(()=>rows.filter(completed).length,[rows]),overdueCount=useMemo(()=>rows.filter(overdue).length,[rows]);
 if(!active||!sessionReady||!logged)return null;
 async function logout(){await supabase.auth.signOut();window.location.href=import.meta.env.BASE_URL;}
 function toggle(id:string){setSelected(s=>s.includes(id)?s.filter(x=>x!==id):[...s,id]);}
 function openAna(){if(!selected.length)return;const q=new URLSearchParams({mode:'do',resource:'tareas',ids:selected.join(',')});navigate(`/ana?${q.toString()}`);}
 function applySort(key:SortKey){if(sortKey===key)setSortDir(v=>v==='asc'?'desc':'asc');else{setSortKey(key);setSortDir('asc');}}
 function sortArrow(key:SortKey){return sortKey===key?(sortDir==='asc'?'↑':'↓'):'';}
 function ariaSort(key:SortKey){return sortKey===key?(sortDir==='asc'?'ascending':'descending'):undefined;}
 const role=ctx?.role||'Usuario';
 return <OperationalShellFrame className="agenda-root" theme={theme} navigation={effectiveNav} activeRoute="/agenda" anaSubtitle="Cuando quieras, avanzamos paso a paso." query={query} onQueryChange={setQuery} searchPlaceholder="Buscar tarea, expediente o responsable..." searchActionLabel="Buscar" onSearchAction={()=>{}} name={role} role="" initials={role.slice(0,2).toUpperCase()} onToggleTheme={()=>setTheme(theme==='light'?'dark':'light')} onLogout={logout} contentClassName="agenda-content">
    <div className="agenda-title"><div><small>AGENDA Y TAREAS</small><h1>Agenda</h1><p>{loading?'Cargando datos autorizados…':status===200?`${rows.length} tareas visibles en tu ámbito autorizado.`:'Lectura canónica según permisos.'}</p></div><div className="agenda-actions"><button onClick={()=>navigate('/tareas/nueva')}>+ Nueva tarea</button><span className={status===200&&!loading?'ops-live ok':'ops-live'}>{loading?'Cargando…':status===200?'Datos vivos':'PRE-PROD'}</span></div></div>
    <section className="agenda-ana-hero"><div className="agenda-ana-photo"><img src={anaVertical} alt="Ana"/></div><div className="agenda-ana-body"><span>ANA · EN ESTA PANTALLA</span><h2>Organizamos lo siguiente, no todo a la vez</h2><p>Selecciona tareas reales y Ana preparará la ejecución para revisión. Nada se completa ni reasigna sin una acción permitida y confirmada.</p><div className="agenda-next"><button onClick={()=>setState(states.find(s=>/pendiente|abierta|hacer/i.test(s))||'')}><b>1</b><strong>Priorizar pendientes</strong><small>Filtrar →</small></button><button onClick={()=>navigate('/ana?mode=help&resource=tareas')}><b>2</b><strong>Ayúdame</strong><small>Revisar agenda →</small></button><button onClick={openAna} disabled={!selected.length}><b>3</b><strong>Lo hace Ana</strong><small>{selected.length?`${selected.length} seleccionadas`:'Selecciona tareas'}</small></button></div></div></section>
    {loading&&<div className="ops-empty" data-testid="agenda-loading"><strong>Cargando…</strong><span>Consultando la fuente canónica de Agenda/Tareas.</span></div>}
    {!loading&&message&&<div className="ops-message" data-testid={status===403?'agenda-forbidden':'agenda-error'}>{message}</div>}
    {!loading&&status===200&&<><section className="agenda-kpis"><article><CalendarDays size={18}/><small>EN FUENTE</small><strong>{rows.length}</strong><span>Registros visibles canónicos</span></article><article><small>PENDIENTES</small><strong>{pendingCount}</strong><span>No completadas según fuente</span></article><article><CheckSquare size={18}/><small>COMPLETADAS</small><strong>{completedCount}</strong><span>Derivado del estado/completada</span></article><article><small>VENCIDAS</small><strong>{overdueCount}</strong><span>Solo si la fecha permite derivarlo</span></article></section>
     <section className="agenda-filter"><div className="agenda-filter-head"><div><small>TRABAJO AUTORIZADO</small><h2>Tareas disponibles</h2></div><span>{visible.length} visibles</span></div><label>BUSCAR<input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Tarea o dato relacionado"/></label><div className="agenda-filter-grid"><label>ESTADO<select value={state} onChange={e=>setState(e.target.value)}><option value="">Todos</option>{states.map(v=><option key={v}>{v}</option>)}</select></label><label>PRIORIDAD<select value={priority} onChange={e=>setPriority(e.target.value)}><option value="">Todas</option>{priorities.map(v=><option key={v}>{v}</option>)}</select></label><label>ORDENAR POR<select value={sortKey} onChange={e=>{setSortKey(e.target.value as SortKey);setSortDir('asc')}}><option value="fecha">Fecha límite</option><option value="tarea">Tarea</option><option value="estado">Estado</option><option value="prioridad">Prioridad</option><option value="responsable">Responsable</option></select></label><button onClick={()=>{setQuery('');setState('');setPriority('');setSelected([]);setSortKey('fecha');setSortDir('asc')}}>Limpiar</button></div></section>
     {selected.length>0&&<section className="agenda-selection"><strong>{selected.length} tareas seleccionadas</strong><span>La selección no modifica datos.</span><button onClick={openAna}>Preparar con Ana</button></section>}
     {visible.length===0?<div className="ops-empty" data-testid="agenda-empty"><strong>Sin tareas visibles</strong><span>No hay registros para estos filtros o para tu ámbito actual.</span></div>:<div className="ops-table-card agenda-table"><div className="ops-table-head"><strong>{visible.length} registros</strong><span>Fuente canónica Notion</span></div><div className="ops-table-wrap ops-sortable-scroll"><table className="ops-sortable-table"><thead><tr><th></th><th data-sortable="true" aria-sort={ariaSort('tarea')}><button className="ops-sort-button" type="button" onClick={()=>applySort('tarea')}>Tarea <span className="ops-sort-arrow">{sortArrow('tarea')}</span></button></th><th data-sortable="true" aria-sort={ariaSort('estado')}><button className="ops-sort-button" type="button" onClick={()=>applySort('estado')}>Estado <span className="ops-sort-arrow">{sortArrow('estado')}</span></button></th><th data-sortable="true" aria-sort={ariaSort('fecha')}><button className="ops-sort-button" type="button" onClick={()=>applySort('fecha')}>Fecha límite <span className="ops-sort-arrow">{sortArrow('fecha')}</span></button></th><th data-sortable="true" aria-sort={ariaSort('prioridad')}><button className="ops-sort-button" type="button" onClick={()=>applySort('prioridad')}>Prioridad <span className="ops-sort-arrow">{sortArrow('prioridad')}</span></button></th><th data-sortable="true" aria-sort={ariaSort('responsable')}><button className="ops-sort-button" type="button" onClick={()=>applySort('responsable')}>Responsable <span className="ops-sort-arrow">{sortArrow('responsable')}</span></button></th><th></th></tr></thead><tbody>{visible.map((r,i)=>{const id=idOf(r);const checked=id&&selected.includes(id);return <tr key={id||i} className={id?'ops-clickable-row':''}><td><input aria-label={`Seleccionar ${titleOf(r)}`} type="checkbox" checked={Boolean(checked)} disabled={!id} onChange={()=>id&&toggle(id)} onClick={e=>e.stopPropagation()}/></td><td onClick={()=>id&&navigate(`/tareas/${encodeURIComponent(id)}`)}><strong>{titleOf(r)}</strong></td><td>{stateOf(r)}</td><td>{dueOf(r)}</td><td>{priorityOf(r)}</td><td>{responsibleOf(r)}</td><td onClick={()=>id&&navigate(`/tareas/${encodeURIComponent(id)}`)}>{id?'→':''}</td></tr>})}</tbody></table></div></div>}</>}
 </OperationalShellFrame>;
}
