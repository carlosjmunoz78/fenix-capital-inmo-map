import {useEffect,useState} from 'react';
import {useLocation,useNavigate} from 'react-router-dom';
import {ArrowLeft,Building2,LogOut,Mail,MapPin,Moon,Phone,Sun} from 'lucide-react';
import {fetchAppApi,supabase} from './supabase';
import {fetchNotariasRuntime} from './notariasRuntime';
import {anaAvatar,fenixLogo} from './assets/visualAssets';
import './operational.css';
import './notarias-polish.css';

type Theme='light'|'dark';type Row=Record<string,unknown>;type Ctx={role?:string};type NavItem={label:string;route:string};
function txt(r:Row|null,k:string){const v=r?.[k];return typeof v==='string'&&v.trim()?v.trim():'No disponible'}
function navOf(d:unknown):NavItem[]{const xs=d&&typeof d==='object'?(d as any).items:null;return Array.isArray(xs)?xs.map((x:any)=>typeof x==='string'?{label:x.replace(/^\//,'')||'Inicio',route:x}:{label:String(x?.label??''),route:String(x?.route??'')}).filter((x:NavItem)=>x.label&&x.route):[]}
const fallbackNav:NavItem[]=[{label:'Inicio',route:'/inicio'}];
export default function NotariaDetailShell(){const location=useLocation(),navigate=useNavigate();const m=location.pathname.match(/^\/notarias\/([^/]+)$/),active=Boolean(m),id=m?.[1]?decodeURIComponent(m[1]):'';const[ready,setReady]=useState(false),[logged,setLogged]=useState(false),[theme,setTheme]=useState<Theme>(()=>(sessionStorage.getItem('fenix-theme') as Theme)||'light'),[ctx,setCtx]=useState<Ctx|null>(null),[nav,setNav]=useState<NavItem[]>([]),[row,setRow]=useState<Row|null>(null),[status,setStatus]=useState<number|null>(null);useEffect(()=>{if(!active)return;supabase.auth.getSession().then(({data})=>{setLogged(Boolean(data.session));setReady(true)});},[active]);useEffect(()=>{if(active){document.documentElement.dataset.theme=theme;sessionStorage.setItem('fenix-theme',theme)}},[active,theme]);useEffect(()=>{if(!active||!logged||!id)return;Promise.all([fetchAppApi<Ctx>('/session/context'),fetchAppApi<unknown>('/navigation'),fetchNotariasRuntime<any>(`/notarias/${encodeURIComponent(id)}`)]).then(([c,n,r])=>{setCtx(c.status===200?c.data:null);setNav(n.status===200?navOf(n.data):[]);setStatus(r.status);setRow(r.status===200?r.data?.item??null:null)});},[active,logged,id]);if(!active||!ready||!logged)return null;const effectiveNav=nav.length?nav:fallbackNav;async function logout(){await supabase.auth.signOut();window.location.href=import.meta.env.BASE_URL;}return <div className="ops-root notarias-root" data-theme={theme}><aside className="ops-side"><button className="ops-brand" onClick={()=>navigate('/inicio')}><img src={fenixLogo} alt=""/><strong>FÉNIX CAPITAL</strong></button><nav>{effectiveNav.map(i=><button key={i.route} className={i.route==='/notarias'?'active':''} onClick={()=>navigate(i.route)}>{i.label}</button>)}</nav><button className="ops-ana" onClick={()=>navigate('/ana')}><img src={anaAvatar} alt="Ana"/><span><strong>Hablar con Ana</strong><small>Apoyo para notaría y firma.</small></span></button></aside><main className="ops-main"><header className="ops-top"><strong>Ficha de notaría</strong><div className="ops-top-actions"><button onClick={()=>setTheme(theme==='light'?'dark':'light')} aria-label="Cambiar tema">{theme==='light'?<Moon size={17}/>:<Sun size={17}/>} {theme==='light'?'Oscuro':'Claro'}</button><div className="ops-profile"><strong>{ctx?.role||'Usuario'}</strong></div><button onClick={logout} aria-label="Cerrar sesión"><LogOut size={17}/></button></div></header><section className="ops-content notarias-content"><button className="inmo-detail-back" onClick={()=>navigate('/notarias')}><ArrowLeft size={15}/> Volver a Notarías</button><div className="ops-title"><div><span className="ops-icon"><Building2 size={20}/></span><div><small>FICHA MAESTRA</small><h1>{row?txt(row,'notaria'):'Notaría'}</h1><p>Fuente canónica Notion · directorio, no registro de firma.</p></div></div><span className={status===200?'ops-live ok':'ops-live'}>{status===200?'Datos vivos':'PRE-PROD'}</span></div>{status===403&&<div className="ops-message">Tu perfil no tiene acceso a esta notaría.</div>}{status===404&&<div className="ops-message">No se ha encontrado la notaría.</div>}{status!==null&&status!==200&&status!==403&&status!==404&&<div className="ops-message">No se pudo cargar la notaría.</div>}{row&&<><section className="profile-card"><div className="profile-main-copy"><h2>{txt(row,'notaria')}</h2><span>{txt(row,'localidad')}</span></div><div className="profile-data"><article><MapPin/><div><small>DIRECCIÓN</small><strong>{txt(row,'direccion')}</strong></div></article><article><Phone/><div><small>TELÉFONO</small><strong>{txt(row,'telefono')}</strong></div></article><article><Mail/><div><small>EMAIL</small><strong>{txt(row,'email')}</strong></div></article></div></section><section className="ops-table-card"><div className="ops-table-head"><strong>Datos de trabajo</strong><span>Sin completar campos ausentes</span></div><div className="ops-message"><p><b>Notario / oficial principal:</b> {txt(row,'notario_oficial_principal')}</p><p><b>Horario / observaciones:</b> {txt(row,'horario_observaciones')}</p><p><b>Notas:</b> {txt(row,'notas')}</p><p><b>Última firma:</b> {txt(row,'ultima_firma')}</p></div></section></>}</section></main></div>}
