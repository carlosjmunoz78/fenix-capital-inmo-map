import {ChangeEvent,useEffect,useMemo,useState} from 'react';
import {createPortal} from 'react-dom';
import {FileScan,FileUp,ShieldCheck,Sparkles,X} from 'lucide-react';
import {useLocation} from 'react-router-dom';
import {IS_PRODUCTION,SUPABASE_PUBLISHABLE_KEY,SUPABASE_URL,supabase} from './supabase';
import {ocrFile,extractDocumentData,type ExtractedDocument} from './browserDocumentOcr';
import {classifyFilename} from './documentClassification';
import {anaVertical} from './assets/visualAssets';

type OriginType='contacto'|'expediente'|'firma'|'inmobiliaria'|'tasacion'|'tarea'|'documento';
type Origin={type:OriginType;code:string;label:string;legacyUpload:boolean};
type Prepare={ok?:boolean;upload_id?:string;storage_path?:string;token?:string;max_bytes?:number};
type Complete={ok?:boolean;document_page_id?:string;reused?:boolean;no_op?:boolean};
type ApplyResult={ok?:boolean;status?:number;error?:string;conflicts?:Array<{field:string;current:string;proposed:string}>;updated?:Record<string,unknown>;cerebro_stored?:boolean};
const BUCKET=IS_PRODUCTION?'fenix-prod-documents':'fenix-preprod-documents-test';
const EVIDENCE=IS_PRODUCTION?'fenix-evidence-api':'fenix-evidence-universal-test';
const INTELLIGENCE=IS_PRODUCTION?'fenix-document-intelligence':'fenix-document-intelligence-test';
const OWN_UPLOAD=new Set<OriginType>(['inmobiliaria','tasacion','tarea','documento']);
const ROUTES:Array<[RegExp,OriginType,string,boolean]>=[
 [/^\/contactos\/([^/]+)$/,'contacto','contacto',true],[/^\/expedientes\/([^/]+)$/,'expediente','expediente',true],[/^\/firmas\/([^/]+)$/,'firma','firma',true],
 [/^\/inmobiliarias\/([^/]+)$/,'inmobiliaria','inmobiliaria',false],[/^\/tasaciones\/([^/]+)$/,'tasacion','tasación',false],[/^\/tareas\/([^/]+)$/,'tarea','tarea',false],[/^\/documentacion\/([^/]+)$/,'documento','documento',false]
];
function originFrom(path:string):Origin|null{for(const[rx,type,label,legacyUpload]of ROUTES){const m=path.match(rx);if(m&&m[1]&&!['nuevo','nueva'].includes(m[1]))return{type,code:decodeURIComponent(m[1]),label,legacyUpload};}return null;}
function mimeOf(file:File){return file.type||(/\.pdf$/i.test(file.name)?'application/pdf':/\.png$/i.test(file.name)?'image/png':/\.webp$/i.test(file.name)?'image/webp':'image/jpeg');}
async function call<T>(fn:string,path:string,init?:RequestInit){const{data:{session}}=await supabase.auth.getSession();if(!session?.access_token)return{status:401,data:null as T|null};let r:Response;try{r=await fetch(`${SUPABASE_URL}/functions/v1/${fn}${path}`,{...init,headers:{'content-type':'application/json',apikey:SUPABASE_PUBLISHABLE_KEY,Authorization:`Bearer ${session.access_token}`,...(init?.headers||{})}})}catch{return{status:0,data:null as T|null}}let data:T|null=null;try{data=await r.json()}catch{}return{status:r.status,data};}
async function preserve(file:File,origin:Origin,declaredType:string){const mime=mimeOf(file);const p=await call<Prepare>(EVIDENCE,'/prepare',{method:'POST',body:JSON.stringify({origin_type:origin.type,origin_code:origin.code,evidence_kind:'documento',filename:file.name,mime_type:mime})});if(p.status!==200||!p.data?.upload_id||!p.data.storage_path||!p.data.token)throw new Error('No se pudo preparar el documento.');if(p.data.max_bytes&&file.size>p.data.max_bytes)throw new Error('El archivo supera el tamaño máximo permitido.');const up=await supabase.storage.from(BUCKET).uploadToSignedUrl(p.data.storage_path,p.data.token,file,{contentType:mime});if(up.error)throw new Error('No se pudo conservar el original.');const d=await call<Complete>(EVIDENCE,'/complete',{method:'POST',body:JSON.stringify({upload_id:p.data.upload_id,title:file.name,tipo:declaredType||'Otro',sensibilidad:'Personal'})});if(d.status!==200||!d.data?.ok)throw new Error('El original se subió, pero no pudo quedar vinculado.');return d.data;}
function fieldLabel(k:string){return k.replace(/_/g,' ').replace(/^./,x=>x.toLocaleUpperCase('es'));}

export default function UniversalDocumentIntelligenceGuard(){
 const loc=useLocation(),origin=useMemo(()=>originFrom(loc.pathname),[loc.pathname]);
 const[host,setHost]=useState<HTMLElement|null>(null),[open,setOpen]=useState(false),[file,setFile]=useState<File|null>(null),[result,setResult]=useState<ExtractedDocument|null>(null),[type,setType]=useState(''),[person,setPerson]=useState(''),[progress,setProgress]=useState(''),[busy,setBusy]=useState(false),[message,setMessage]=useState(''),[conflicts,setConflicts]=useState<ApplyResult['conflicts']>([]),[overwrite,setOverwrite]=useState(false),[ana,setAna]=useState('');
 useEffect(()=>{if(!origin){setHost(null);return;}const h=document.createElement('div');h.className='universal-doc-intelligence-host';const place=()=>{const content=document.querySelector<HTMLElement>('.ops-content,.dir-content');if(!content)return;const upload=content.querySelector<HTMLElement>('.context-evidence-inline-host');if(upload?.parentElement===content){if(upload.nextElementSibling!==h)content.insertBefore(h,upload.nextElementSibling);return;}const kpis=content.querySelector<HTMLElement>(':scope > [class$="-kpis"], :scope > [class*="-kpis"]');const hero=content.querySelector<HTMLElement>(':scope > [class*="-ana-hero"], :scope > .ops-ana-card');const anchor=kpis??hero;if(anchor){if(anchor.nextElementSibling!==h)content.insertBefore(h,anchor.nextElementSibling);}else if(h.parentElement!==content)content.insertBefore(h,content.firstChild);};place();const ob=new MutationObserver(place);ob.observe(document.body,{childList:true,subtree:true});setHost(h);return()=>{ob.disconnect();h.remove();setHost(null)}},[origin?.type,origin?.code]);
 if(!origin||!host)return null;
 function reset(){setFile(null);setResult(null);setType('');setPerson('');setProgress('');setMessage('');setConflicts([]);setOverwrite(false);setAna('');}
 async function read(f:File){setBusy(true);setResult(null);setMessage('');try{const o=await ocrFile(f,setProgress);const r=extractDocumentData(o.text,o.confidence);setResult(r);setProgress(`Lectura terminada${o.confidence!==null?` · confianza ${Math.round(o.confidence)}%`:''}.`);}catch(e){setMessage(e instanceof Error?e.message:'No se pudo leer el documento.');}finally{setBusy(false)}}
 async function choose(e:ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0]||null;e.target.value='';if(!f)return;reset();setFile(f);const c=classifyFilename(f.name);setType(c.type);setPerson(c.person);if(!c.complete){setAna(`Necesito que me indiques qué documento es y a qué persona o referencia pertenece “${f.name}”. Puedes mantener el archivo, pero no lo leeré ni lo clasificaré a ciegas.`);setProgress('Clasificación pendiente de Ana.');return;}setProgress(`Nombre correcto: ${c.type} · ${c.person}. Leyendo…`);await read(f);}
 async function classifyAndRead(){if(!file||!type.trim()||!person.trim()||busy)return;setAna('');setProgress(`Clasificación confirmada: ${type.trim()} · ${person.trim()}. Leyendo…`);await read(file);}
 async function apply(){if(!file||!result||!type.trim()||!person.trim()||busy)return;setBusy(true);setMessage('Conservando original y vinculando la lectura a CEREBRO…');try{const saved=await preserve(file,origin,type.trim());if(saved.reused||saved.no_op){setAna(`Este documento ya existe en ${origin.label}. No he creado otra copia ni he vuelto a consolidar sus datos. Revisa el documento existente si quieres comprobarlo.`);setMessage('Documento repetido · no-op seguro.');return;}const payload={origin_type:origin.type,origin_code:origin.code,fields:result.fields,summary:result.summary,document_type:result.documentType,declared_document_type:type.trim(),declared_person:person.trim(),document_page_id:saved.document_page_id||null,confidence:result.confidence,confirm_overwrite:overwrite};const r=await call<ApplyResult>(INTELLIGENCE,'/apply',{method:'POST',body:JSON.stringify(payload)});if(r.status===409&&r.data?.conflicts?.length){setConflicts(r.data.conflicts);setAna('He encontrado datos distintos a los que ya existen. Revísalos y confirma expresamente si quieres sustituirlos.');setMessage('Conflicto pendiente de confirmación.');return;}if(r.status!==200||!r.data?.ok)throw new Error(r.data?.error||`No se pudo aplicar la lectura (${r.status}).`);setConflicts([]);setOverwrite(false);setMessage('Original vinculado · lectura guardada en CEREBRO · campos canónicos actualizados cuando corresponde.');}catch(e){setMessage(e instanceof Error?e.message:'No se pudo completar el tratamiento.');}finally{setBusy(false)}}
 async function plainUpload(e:ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0]||null;e.target.value='';if(!f)return;const c=classifyFilename(f.name);if(!c.complete){setOpen(true);reset();setFile(f);setType(c.type);setPerson(c.person);setAna(`Antes de subir “${f.name}” necesito tipo de documento y persona/referencia. Dímelo aquí para que quede bien clasificado.`);return;}setBusy(true);try{const saved=await preserve(f,origin,c.type);if(saved.reused||saved.no_op){setOpen(true);reset();setFile(f);setType(c.type);setPerson(c.person);setAna(`Este documento ya estaba guardado en ${origin.label}. No he creado una copia duplicada.`);setMessage('Documento repetido · no-op seguro.');}else{setOpen(true);reset();setFile(f);setType(c.type);setPerson(c.person);setProgress('Documento guardado. Ahora lo leo para extraer y rellenar los datos.');await read(f);}}catch(err){setOpen(true);setMessage(err instanceof Error?err.message:'No se pudo subir el documento.');}finally{setBusy(false)}}
 const ownUpload=OWN_UPLOAD.has(origin.type)&&!origin.legacyUpload;
 const ready=Boolean(type.trim()&&person.trim());
 const controls=<div className="universal-doc-controls">{ownUpload&&<label className="universal-upload"><FileUp size={17}/> Subir documentos<input type="file" accept="application/pdf,image/png,image/jpeg,image/webp" onChange={e=>void plainUpload(e)}/></label>}<button type="button" data-testid="intelligent-document-open" className="universal-read" onClick={()=>{reset();setOpen(true)}}><FileScan size={17}/> Ana · leer documento y rellenar datos</button></div>;
 return <>{createPortal(controls,host)}{open&&createPortal(<div className="universal-doc-backdrop"><section role="dialog" aria-modal="true" aria-label="Lectura inteligente de documento" className="universal-doc-card"><header><div className="universal-ana"><img src={anaVertical} alt="Ana"/><div><span>ANA · CEREBRO DOCUMENTAL</span><h2>Leo, clasifico y preparo los datos</h2><p>El original queda intacto. No sobrescribo datos distintos sin confirmación.</p></div></div><button onClick={()=>setOpen(false)} aria-label="Cerrar"><X size={18}/></button></header>{ana&&<div className="universal-ana-alert" role="alert"><strong>Ana</strong><p>{ana}</p></div>}<label className="universal-picker"><FileScan size={19}/>{busy?'Procesando…':'Elegir foto o PDF'}<input type="file" accept="application/pdf,image/png,image/jpeg,image/webp" onChange={e=>void choose(e)} disabled={busy}/></label>{file&&<><small>Archivo: {file.name}</small><div className="universal-class"><label>Tipo de documento<input aria-label="Tipo de documento" value={type} onChange={e=>{setType(e.target.value);setResult(null)}} placeholder="Oferta bancaria, tasación, nómina…"/></label><label>Persona / referencia<input aria-label="Persona / referencia" value={person} onChange={e=>{setPerson(e.target.value);setResult(null)}} placeholder="Nombre y apellidos o referencia"/></label>{!result&&<button disabled={!ready||busy} onClick={()=>void classifyAndRead()}>Continuar con OCR</button>}</div></>}{progress&&<div className="universal-status">{progress}</div>}{result&&<><article><strong>TIPO DETECTADO</strong><p>{result.documentType}</p><strong>RESUMEN</strong><p>{result.summary}</p></article><div className="universal-fields"><strong>DATOS EXTRAÍDOS</strong>{Object.entries(result.fields).length?Object.entries(result.fields).map(([k,v])=><div key={k}><span>{fieldLabel(k)}</span><b>{String(v)}</b></div>):<p>No hay datos suficientemente fiables para autorrellenar.</p>}</div><details><summary>Ver texto leído</summary><pre>{result.rawText||'Sin texto legible'}</pre></details></>}{conflicts?.length?<article className="universal-conflicts"><strong>CONFLICTOS</strong>{conflicts.map(c=><p key={c.field}>{fieldLabel(c.field)}: actual <b>{c.current}</b> → documento <b>{c.proposed}</b></p>)}<label><input type="checkbox" checked={overwrite} onChange={e=>setOverwrite(e.target.checked)}/> Confirmo la sustitución de estos datos.</label></article>:null}{message&&<div className="universal-status" role="status">{message}</div>}<footer><ShieldCheck size={17}/><span>Original + procedencia + resumen + extracción quedan trazables.</span><button className="primary" disabled={!file||!result||!ready||busy||Boolean(conflicts?.length&&!overwrite)} onClick={()=>void apply()}>{busy?'Procesando…':conflicts?.length?'Confirmar conflictos y aplicar':'Confirmar y aplicar datos'}</button></footer></section></div>,document.body)}<style>{`
.universal-doc-intelligence-host{margin-top:10px}.universal-doc-controls{display:grid;gap:8px}.universal-upload,.universal-read{width:100%;border-radius:12px;padding:12px 16px;display:flex;align-items:center;justify-content:center;gap:8px;font-weight:800;cursor:pointer}.universal-upload{background:#f4741f;color:#fff;border:1px solid #f4741f}.universal-upload input{display:none}.universal-read{background:var(--panel,#fff);color:#5c2fa8;border:1px solid #7d4bd1}.universal-doc-backdrop{position:fixed;inset:0;z-index:10030;background:rgba(18,15,23,.62);display:grid;place-items:center;padding:18px}.universal-doc-card{width:min(800px,96vw);max-height:92vh;overflow:auto;background:var(--panel,#fff);color:var(--text,#1d1d1f);border-radius:20px;padding:20px;border:1px solid #dacced;box-shadow:0 28px 80px rgba(0,0,0,.28)}.universal-doc-card header{display:flex;justify-content:space-between;gap:14px}.universal-doc-card header>button{border:0;background:transparent;color:inherit;cursor:pointer}.universal-ana{display:flex;gap:13px;align-items:center}.universal-ana img{width:58px;height:70px;object-fit:cover;object-position:top;border-radius:12px}.universal-ana span{font-size:11px;font-weight:900;color:#6b39bb;letter-spacing:.06em}.universal-ana h2{margin:4px 0;font-size:21px}.universal-ana p{margin:0;font-size:12px;color:var(--muted,#686870)}.universal-ana-alert{margin-top:14px;padding:13px;border:1px solid #b795e4;background:rgba(107,57,187,.08);border-radius:13px}.universal-ana-alert p{margin:5px 0 0;line-height:1.45}.universal-picker{margin-top:15px;border:1px dashed #8d65cf;border-radius:13px;padding:14px;display:flex;justify-content:center;gap:8px;font-weight:800;cursor:pointer}.universal-picker input{display:none}.universal-class{display:grid;grid-template-columns:1fr 1fr;gap:9px;margin-top:12px}.universal-class label{display:grid;gap:5px;font-size:12px;font-weight:700}.universal-class input{border:1px solid #d8d1e4;background:var(--panel,#fff);color:inherit;border-radius:9px;padding:10px}.universal-class button{grid-column:1/-1;justify-self:start;border:0;background:#6b39bb;color:#fff;border-radius:9px;padding:9px 13px;font-weight:800}.universal-status,.universal-doc-card article,.universal-fields{margin-top:12px;border:1px solid var(--line,#e6e2e8);border-radius:12px;padding:12px 14px;background:rgba(120,110,130,.05)}.universal-doc-card article p{margin:5px 0 10px}.universal-fields>div{display:flex;justify-content:space-between;gap:14px;border-top:1px solid var(--line,#e6e2e8);padding:8px 0}.universal-doc-card pre{white-space:pre-wrap;max-height:180px;overflow:auto;font-size:11px}.universal-conflicts{border-color:#e7a37c!important;background:#fff7f1!important}.universal-doc-card footer{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:15px}.universal-doc-card footer span{font-size:11px;color:var(--muted,#686870)}.universal-doc-card footer .primary{margin-left:auto;border:0;background:#6b39bb;color:#fff;border-radius:10px;padding:11px 15px;font-weight:800}.universal-doc-card button:disabled{opacity:.45;cursor:not-allowed}@media(max-width:650px){.universal-class{grid-template-columns:1fr}.universal-doc-card footer .primary{width:100%;margin-left:0}}
`}</style></>;
}
